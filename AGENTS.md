# ChatOMS 프로젝트 규칙

## 프로젝트 목적

- ChatOMS는 공식 Claude Code CLI와 공식 Codex CLI를 조율하는 로컬 데스크톱 AI 코딩 하네스다.
- Windows를 우선 지원하고 macOS를 지원한다.
- 기술 스택은 Tauri 2, React, TypeScript strict, Rust, SQLite다.

## 요구사항 기준

- 상세 제품 요구사항은 `docs/PRODUCT_REQUIREMENTS.md`를 기준으로 한다.
- 확정되지 않은 기능을 임의로 추가하지 않는다.
- 요구사항과 구현이 충돌하면 구현 전에 사용자에게 보고한다.

## 문서 우선순위

1. `docs/PRODUCT_REQUIREMENTS.md`의 제품 요구사항
2. `docs/DECISIONS.md`, `docs/SECURITY_POLICY.md`, `docs/STATE_MACHINE.md`의 확정 설계·보안·상태 불변조건
3. `docs/PHASE_PLAN.md`의 구현 단계와 범위
4. 이 파일의 현재 작업 규칙
5. `README.md`의 사용자·개발자 안내

같은 단계에서 충돌하거나 구현이 상위 문서와 다르면 임의로 해석하거나 구현하지 말고 사용자에게 보고한다. 이전 Phase 설명은 historical record이며 현재 작업 규칙이 아니다.

## 현재 구현 범위

- Phase 1~3은 완료되었고, 현재 승인된 구현 범위는 Phase 4 Unit 1의 provider-neutral `TaskState` rename과 SQLite forward migration, Unit 2의 `WorkKind` 및 provider eligibility 읽기 모델, Unit 3의 실제 provider CLI 실행 없이 fixture 기반으로 검증된 streaming/cancellation ProcessRunner 기반, Unit 4a-1의 task 생성 경계에 결합된 immutable `TaskBrief`(requirements/completionCriteria/prohibitedScope) 저장 기반, Unit 4a-2의 `TaskBrief` 입력 폼과 필수 검증 기반, Unit 4b-1의 Claude Planning 전송 동의 저장과 `WorktreeReady → Planning` 원자적 전이 기반, Unit 4b-2의 Claude Planning 실행 adapter 기반, Unit 4b-3의 Claude Planning 최종 결과 저장과 상태 전이 기반, Unit 4b-4의 Claude Planning 시작·취소 Tauri IPC/UI 배선과 `Planning → Cancelled` 직접 전이 기반, Unit 4b-5의 앱 재시작 시 leftover `Planning` task를 `RecoveryRequired`로 되돌리는 startup reconciliation 기반, Unit 4b-6의 Claude Planning background thread panic containment 기반, Unit 4b-7의 `AwaitingDesignApproval` task에 대한 저장된 Claude Planning 결과 read-only 표시 기반, Unit 4c-1의 Claude Implementation 전송 동의 저장과 `AwaitingDesignApproval → Implementing` 원자적 전이 기반, Unit 4c-2의 Claude Implementation 실행 adapter 기반, Unit 4c-3의 Claude Implementation 결과 저장과 `Implementing → Testing/Paused/RecoveryRequired` 상태 전이 기반, Unit 4c-4의 Claude Implementation adapter를 provider port·application orchestration에 연결한 backend 실행 경로 기반, Unit 4c-5의 Claude Implementation 시작·취소 Tauri IPC/UI 배선 기반, Unit 4d-1의 Testing 검증 명령 후보 discovery와 1회 사용자 승인 저장 기반, Unit 4d-1b의 승인된 검증 명령 executable을 사용자 승인 절대경로·Windows stable file identity에 결합해 저장하는 기반이다. Unit 4e-4a는 Review stdin 전용 ephemeral Git diff read capability, Unit 4e-4b는 Claude Review read-only 실행 adapter 기반이다.
- Unit 4a-2의 구현: ProjectsPage에서 task 생성 전 세 TaskBrief 필드(requirements, completionCriteria, prohibitedScope)를 입력받고, 빈 값이면 요청을 보내지 않으며, IPC/Tauri boundary에서도 필수 검증으로 UI 우회를 방지한다.
- Unit 4b-1의 구현: `task_provider_consents` immutable 테이블과 `TaskService::start_planning` 유스케이스로 Claude Planning 1회 전송 동의를 기록·재사용하고, 동일 task version에 유효한 동의가 있으면 재사용하며, 동의 기록·`WorktreeReady → Planning` 상태 갱신·transition history 저장을 하나의 SQLite transaction으로 처리한다. 실제 Claude CLI 실행, Tauri IPC 노출과 동의 UI는 포함하지 않는다.
- Unit 4b-2의 구현: `crates/chatoms-infrastructure/src/claude_planning.rs`의 `ClaudePlanningAdapter`가 Unit 3 `StreamingProcessRunner`로 Claude Planning을 실행한다. spawn 직전 매번 `ProviderCapabilityPort`(기존 trust/compatibility/login/preflight 게이트)를 다시 호출해 캐시를 신뢰하지 않고, 실패 시 typed `PreflightRejected` 결과만 반환하며 subprocess를 시작하지 않는다. CWD는 app-owned trusted preflight directory이고 task worktree는 `--add-dir` 읽기 전용 인자로만 전달하며, TaskBrief 세 필드는 고정 템플릿으로 stdin에만 실린다. `ClaudePlanningObserver`는 raw stdout 콜백이 없는 content-free 이벤트만 노출한다.
- Unit 4b-3의 구현: 공식 문서로 `-p --permission-mode plan`이 헤드리스에서 승인 대화상자 없이 계획을 stdout에 출력하고 정상 종료함을 확인했고, argv에 `--output-format json`을 추가해 종료 시 단일 JSON envelope만 받는다. `ClaudePlanningAdapter`는 raw stdout을 호출부에 노출하지 않고 내부에서만 bounded 버퍼로 누적한 뒤 `subtype`/`is_error`/`result`/`num_turns` 필드만 신뢰하는 최소 스키마로 파싱하고, `SecretRedactor::redact_text`로 마스킹·크기 제한한 뒤 `ClaudePlanningResult`(content-free `PlanningResultOutcome` + 안전한 plan text)를 반환한다. session_id·비용·기타 필드는 파싱 대상이 아니라 애초에 포섭되지 않는다. `crates/chatoms-infrastructure/migrations/0007_task_planning_results.sql`이 `task_id` 1:1, provider/work_kind 고정, outcome CHECK, plan_text 존재 규칙을 가진 immutable 테이블을 추가한다. `TaskService::record_planning_result`가 결과 저장과 상태 전이(`Completed→AwaitingDesignApproval`, `Failed→Failed`, `RecoveryRequired→RecoveryRequired`)를 하나의 repository transaction으로 처리하고, 원자적 쓰기 자체가 실패하면 결과 행 없이 `Planning→RecoveryRequired`로 fallback한다. StreamingOutcome 매핑: `Completed`+`exit_code==0`+유효 스키마→`Completed`, `Completed`+nonzero 또는 `StdoutBoundExceeded`→`Failed`, `Cancelled`→`Cancelled`, `Uncertain`→`RecoveryRequired`(Planning의 `--tools Read,Glob,Grep` 계약상 외부 쓰기가 구조적으로 불가능하므로 `UnknownExternalEffect`는 이 Unit에서 도달 불가로 판단). 실제 Claude/Codex CLI, 로그인 probe와 configured executable은 테스트에서 실행하지 않는다.
- Unit 4b-4의 구현: 사용자 승인으로 `docs/STATE_MACHINE.md`의 `Planning` 행에 `Cancelled`를 직접 다음 상태로 추가했다(다른 active-execution 상태와 달리 `Paused`를 거치지 않음; 근거는 STATE_MACHINE.md 본문 참고). `chatoms-domain`의 `can_transition_to`에 이 edge를 반영했고, `TaskService::record_planning_result`는 이제 `PlanningResultOutcome::Cancelled`를 `TaskState::Cancelled`로 매핑해 결과 저장·상태 전이·lease 해제를 원자적으로 처리한다(과거의 `Unsupported` fallback은 제거됨). `chatoms-ports::planning::ClaudePlanningExecutor` port와 `chatoms-infrastructure`의 `ClaudePlanningAdapter` 구현체를 추가해 provider 실행을 port/adapter로 분리했고, `chatoms-application::planning_execution`의 `PlanningExecutionStarter::begin`(fresh capability 재확인 + `TaskService::start_planning` 원자 전이 + worktree/brief 조회, 거부 시 상태 불변)과 `PlanningExecutionRecorder::run_and_record`(provider 실행 + `TaskService::record_planning_result`, preflight 거부·executor 실패는 `RecoveryRequired`로 fallback)가 이를 연결한다. Tauri의 `start_claude_planning` 명령은 동의·전이를 동기적으로 수행해 즉시 `TaskDto`를 반환하고, 실제 provider 실행과 결과 기록은 detached background thread에서 진행하며, 메모리 전용 `PlanningRunRegistry`(task별 `AtomicCancellationSignal`)가 동시 실행 중인 `cancel_claude_planning` 요청이 그 thread에 도달하도록 한다. 취소 요청은 상태를 직접 바꾸지 않고, streaming runner가 확인한 결과만 `Cancelled`(확인된 취소) 또는 `RecoveryRequired`(불확실한 취소, lease 유지)로 기록된다. ProjectsPage는 `WorktreeReady`에서 `get_provider_eligibility` 결과로 Start 버튼을 게이팅하고 고정 blocker 문구만 표시하며, 전송 동의 확인 화면을 거친 뒤 시작하고, `Planning` 중에는 취소 버튼과 polling 기반 진행 상태를, 종료 상태에는 안전한 상태 문구만 표시한다. 이 과정에서 `RepositoryHandle`(src-tauri)이 `get_claude_binding`/`ensure_default_profile_and_claude_binding`/`update_claude_executable_path`를 위임하지 않고 있던 Phase 3의 기존 버그를 발견해 함께 수정했다(해당 세 메서드가 트레이트 기본값 `OperationFailed`로 빠져 프로덕션에서 항상 실패하고 있었음).
- Unit 4b-4는 Codex, Claude write, resume/handoff, `PlanReady`, Context Package 확장, planning 결과 상세 뷰, 고위험 승인 화면, 별도 execution/session table을 포함하지 않는다.
- Unit 4b-5의 구현: `TaskService::reconcile_startup_planning`(`crates/chatoms-application/src/tasks.rs`)이 `PlanningRunRegistry`가 메모리 전용이라 앱 재시작 뒤에는 재사용·자동 재실행할 실행 핸들이 없다는 근거로, 활성 lease의 task가 `Planning`이면 기존 `mark_recovery_required` 경로(정적 전이와 history 저장을 하나의 repository transaction으로 처리하며 non-terminal이므로 `ActiveTaskLease`를 유지)를 그대로 재사용해 `RecoveryRequired`로 전이한다. 활성 task가 없거나 `Planning`이 아니면 no-op이고, repository 오류는 그대로 전파해 성공으로 추정하지 않는다. `src-tauri/src/bootstrap.rs`의 `compose_runtime`이 기존 `GitIsolationService::reconcile_startup` 직후 이를 호출하며, 실패하면 기존 Git reconciliation과 동일하게 `ManagedRuntime::unavailable`로 fail-closed한다. `PlanningRunRegistry::register`(`src-tauri/src/state.rs`)는 이미 등록된 task id를 덮어쓰지 않고 `None`을 반환해 충돌을 거부하도록 수정했고, `handle_start_claude_planning`은 이 `None`을 `WorktreeReady → Planning` 전이가 이미 커밋된 뒤의 invariant 위반으로 취급해 `TaskService::record_planning_result`로 `RecoveryRequired`에 즉시 fallback한다. `cancel_claude_planning`이 registry에서 실행을 찾지 못하면(`requested: false`) ProjectsPage가 이를 오류로 취급해 상태 새로고침을 안내하는 안전한 복구 문구를 표시한다.
- Unit 4b-5는 provider 재실행, session resume/handoff, process 탐색·종료, 별도 execution/session table, Codex, Claude write와 planning 결과 상세 UI를 포함하지 않는다.
- Unit 4b-6의 구현: `PlanningExecutionRecorder::run_and_record_with_panic_containment`(`crates/chatoms-application/src/planning_execution.rs`)가 `run_and_record`(executor 호출) 전체를 `std::panic::catch_unwind` + `AssertUnwindSafe`로 감싸고, panic을 잡으면 payload를 검사·포맷·저장하지 않고 즉시 버린 뒤 기존 `TaskService::record_planning_result`(`PlanningResultOutcome::RecoveryRequired`) 경로를 그대로 재사용해 `Planning → RecoveryRequired` 전이·history 기록·lease 유지를 수행한다. 이 recovery 기록 자체가 실패하면(예: stale task version) 성공으로 추정하지 않고 그 오류를 그대로 반환하며, task는 `Planning`으로 남을 수 있고 Unit 4b-5의 `reconcile_startup_planning`이 다음 기동에서 회수한다. `src-tauri/src/commands/planning.rs`의 background thread는 이제 이 containment 경로를 호출하고, `PlanningRunRegistry` unregister는 명시적 호출 대신 `UnregisterOnDrop` RAII guard(스레드 진입 직후 생성)의 `Drop`으로 옮겨, panic 발생 위치·유무와 무관하게 unregister가 항상 실행되도록 보장한다(이 guard 자체는 순수 in-memory 정리이므로 panic 시에도 실패하지 않는다). 그 결과 `cancel_claude_planning`은 이미 죽은 thread를 가리키는 stale registry entry를 더 이상 "찾음"으로 잘못 보고하지 않는다. Rust의 기본 panic hook이 찍는 1줄 stderr 메시지 자체를 전역적으로 억제하는 process-wide `panic::set_hook` 변경은 이 Unit의 좁은 범위를 넘는 것으로 판단해 도입하지 않았으며, 이 앱 자신의 UI/DTO/DB/structured 로그 표면에는 panic payload가 어떤 경로로도 도달하지 않는다.
- Unit 4b-6은 실제 Claude/Codex CLI 실행, session/execution table, resume/handoff, provider 변경, UI 기능 확장과 전역 panic hook 변경을 포함하지 않는다.
- Unit 4b-7의 구현: `FoundationRepository::get_task_planning_result`(`crates/chatoms-ports/src/repository.rs`)와 그 SQLite 구현(`load_planning_result`, `crates/chatoms-infrastructure/src/database/repository.rs`)이 `task_planning_results`의 immutable 저장값을 그대로 읽어오고, `TaskService::get_planning_result`(`crates/chatoms-application/src/tasks.rs`)가 이를 `PlanningResultView`로 노출한다. Tauri의 `get_planning_result` 명령(`src-tauri/src/commands/planning.rs`)은 task를 조회해 현재 상태가 `AwaitingDesignApproval`일 때만 결과를 반환하고, 그 외 상태(및 결과 미기록)는 동일하게 `Ok(None)`으로 취급해 이 read-only 표면이 `record_planning_result`의 outcome-to-state 매핑에 의존하지 않게 한다. `PlanningResultDto`(`src-tauri/src/dto/planning_result.rs`)는 저장된 plan_text·outcome·exit_code·turn_count·시각만 그대로 직렬화하며 재마스킹·재파싱·재실행을 하지 않는다. 프런트엔드는 `src/ipc/planning_result.ts`의 런타임 가드로 unknown/malformed 응답을 거부하고, ProjectsPage는 `awaitingDesignApproval`에서만 결과를 조회해 읽기 전용으로 표시하며 결과 없음·조회 실패를 raw 오류 없는 안전한 빈 상태/오류 상태로 표시한다.
- Unit 4b-7은 "계속하기"·재계획·구현 시작·provider 재실행 UI, 고위험 승인 화면, planning 결과 수정·삭제, Codex, Claude write, resume/handoff, 별도 execution/session table을 포함하지 않는다.
- Unit 4c-1의 구현: `crates/chatoms-infrastructure/migrations/0008_implementation_consents.sql`이 `task_provider_consents`의 `work_kind` CHECK를 `'Planning'`에서 `'Planning' | 'Implementation'`으로 넓히는 forward-only 테이블 재생성 migration을 추가한다(기존 0006 migration은 immutable이므로 직접 수정하지 않음; 기존 Planning consent 행은 데이터 보존). `FoundationRepository::save_implementation_transition`(port 기본값은 여전히 `OperationFailed`)이 `SqliteFoundationRepository`에 구현되고 `SharedFoundationRepository`·`RepositoryHandle` 두 wrapper 모두에 위임이 추가됐다(Unit 4b-4에서 발견된 위임 누락 재발 방지를 위해 `src-tauri/tests/repository_handle_wiring.rs`에 동일한 실제 SQLite wrapper-chain 테스트를 추가). `TaskService::start_implementation`이 `AwaitingDesignApproval` 상태·task version을 검증하고, `(task_id, Claude, Implementation, task_version)` 동의를 재사용하거나 새로 기록하며, 동의 기록(있는 경우)·`Implementing` 상태 갱신·transition history 저장을 하나의 SQLite transaction으로 처리한다. Planning 동의와 Implementation 동의는 work_kind가 복합 키의 일부이므로 완전히 독립적으로 저장·조회된다. Implementing은 non-terminal이므로 `ActiveTaskLease`를 유지한다. 실제 Claude/Codex CLI 실행, Claude write adapter/argv/stdin/ProcessRunner 연결, 결과 저장 table, `Implementing → Testing` 전이, IPC/UI 배선은 포함하지 않는다.
- Unit 4c-2의 구현: `crates/chatoms-infrastructure/src/claude_implementation.rs`의 `ClaudeImplementationAdapter`가 Unit 3 `StreamingProcessRunner`로 Claude Implementation을 실행하는 adapter만 추가한다(port 분리, 결과 영속, `Implementing → Testing/RecoveryRequired` 상태 전이, IPC/UI는 이번 Unit에 없음 — Unit 4b-2가 4b-4 이전에 그랬던 것과 동일한 분리). 공식 Claude Code CLI 문서(`code.claude.com/docs/en/{permission-modes,cli-reference,headless,permissions}`)로 확인한 argv: `--permission-mode default`(reads만 자동 승인) + `--tools Read,Glob,Grep,Edit,Write`(Bash를 포함한 다른 모든 도구를 세션에서 구조적으로 제거— `--allowedTools`는 도구를 미승인 상태로 남기고 존재 자체는 없애지 않으므로 이 목적에는 `--tools`가 맞는 flag임을 문서로 확인) + `--allowedTools Edit,Write`(reads는 default mode에서 이미 자동 승인되므로 write 도구 두 개만 headless 승인 필요) + `--add-dir <worktree>`(읽기+쓰기 권한, CWD는 변경하지 않음) + `--output-format json` + `--max-turns 20`(CLI 자체 강제) + `--strict-mcp-config`(`--mcp-config` 없이 사용해 MCP 서버 0개 보장) + `--setting-sources project,local`(`user` 제외로 `~/.claude/settings.json`의 hook 로드 차단; managed settings는 이 flag로 막을 수 없으며 이는 기존 "회사 장비 정책 우선" 원칙의 연장으로 판단) + `--disable-slash-commands`. `--add-dir` 대상 directory는 `enabledPlugins`/`extraKnownMarketplaces` 키를 제외한 `.claude/settings.json`을 로드하지 않으며 `CLAUDE.md`는 `CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1`일 때만 로드됨을 문서로 확인했다. 이 환경변수가 상속된 부모 프로세스 환경에 우연히 설정되어 있어도 항상 제거되도록, Claude 전용이 아닌 `chatoms-infrastructure`의 공유 `StdProcessRunner::run_streaming` 자체에 `env_remove`를 추가했다(Planning도 함께 적용받음). CWD는 계속 app-owned trusted preflight directory다. TaskBrief 3필드와 저장된 plan text는 argv가 아니라 stdin 고정 템플릿에만 실리며, plan text는 템플릿 안에서 "AI-generated, untrusted" 로 명시하고 고정 지시문도 이를 맹신하지 말라고 명시한다. Implementation 전용 stdin 총 크기 상한(512KiB, 저장 가능한 최대 plan_text 100,000자보다 넉넉히 크고 CLI 자체의 10MiB 한도보다는 훨씬 작음)을 spawn 전에 검사해 초과 시 `ClaudeImplementationStartOutcome::StdinTooLarge`로 fail-closed하고 subprocess를 시작하지 않는다. CLI에 wall-clock timeout flag가 없으므로 30분 상한은 adapter가 직접 구현한다: 호출자의 `CancellationSignal`과 30분 뒤 시각을 합성하는 `DeadlineCancellationSignal`이 기존 `StreamingProcessRunner::run_streaming`의 협조적 polling에 자연스럽게 편입되며, 포트 시그니처 변경이나 별도 타이머 스레드가 없다. Implementation 전용 `ImplementationResultOutcome`은 Planning의 `PlanningResultOutcome`과 달리 `Failed`가 없다: write는 실제 파일 변경을 남길 수 있어 nonzero exit·손상된 JSON·stdout 상한 초과·미확인 취소 어느 것도 "실패했으니 안전하게 버려도 된다"고 확정할 수 없으므로 이 모든 경로를 `RecoveryRequired`로 fail-closed하며, confirmed cancellation만 `Cancelled`로 남긴다. 이 상태 매핑을 실제 `Implementing → Testing/RecoveryRequired` Task 상태 전이에 연결하는 것은 다음 Unit이다.
- Unit 4c-2는 Bash 및 기타 검증 명령 실행, 결과 저장 migration/table, `Implementing → Testing`/`RecoveryRequired` 상태 전이, IPC/UI, cancel/pause/startup reconciliation, Codex, provider 자동 전환, resume/handoff를 포함하지 않는다.
- Unit 4c-3의 구현: `crates/chatoms-infrastructure/migrations/0009_task_implementation_results.sql`이 `task_id` 1:1, provider(Claude)/work_kind(Implementation) 고정, outcome CHECK(`Completed`/`Cancelled`/`RecoveryRequired`, Planning과 달리 `Failed` 없음)를 가진 immutable 테이블을 추가한다. 이 테이블은 transcript, stdout/stderr, tool I/O, prompt, plan text, diff, executable path, login/session 정보는 물론 outcome별 content 필드도 전혀 저장하지 않으며, 실제 변경 내역은 task worktree/Git diff가 유일한 source of truth로 남는다. `chatoms_ports::repository`에 `ImplementationResultOutcome`(Unit 4c-2 adapter가 정의한 동명의 로컬 타입과는 별개— Planning이 4b-2/4b-3에서 그랬던 것과 달리 이번에는 adapter 타입을 포트로 승격하지 않고, 어댑터와의 연결은 이후 Unit으로 미룸)과 `TaskImplementationResultRecord`, 그리고 `FoundationRepository::save_implementation_result`/`get_task_implementation_result` 최소 메서드를 추가했다. `SqliteFoundationRepository::save_implementation_result`는 task가 `Implementing`인지 검증하고 결과 행 삽입·task 상태 갱신·transition history 삽입을 하나의 SQLite transaction으로 처리하며, 대상 상태(`Testing`/`Paused`/`RecoveryRequired`) 중 어느 것도 terminal이 아니므로 `save_planning_result`와 달리 `ActiveTaskLease`를 절대 해제하지 않는다. `TaskService::record_implementation_result`가 outcome을 승인된 상태로 매핑한다: `Completed`는 `Task::transition_to(Testing)`, 확인된 `Cancelled`는 `Task::transition_to`가 아니라 `Task::pause()`를 사용해 `Paused`로 진입하며 `resume_target_state = Implementing`을 자동으로 설정하고(`docs/STATE_MACHINE.md`가 이미 `Implementing → Paused`를 문서화한 기존 contextual pause 경로이므로 상태 머신 문서·`can_transition_to` 변경이 필요 없었음), 그 외 모든 fail-closed outcome은 `RecoveryRequired`로 fallback한다. 원자적 쓰기 자체가 실패하면 기존 `recover_after_implementation_persistence_failure`가 결과 행 없이 `Implementing → RecoveryRequired`로 fallback하고, 그 fallback마저 실패하면 오류를 그대로 전파해 성공이나 terminal 상태로 추정하지 않는다. `SharedFoundationRepository`(chatoms-infrastructure)와 `RepositoryHandle`(src-tauri) 두 wrapper 모두에 새 메서드 위임을 추가했고, `src-tauri/tests/repository_handle_wiring.rs`에 실제 SQLite wrapper-chain을 통한 저장·조회 테스트를 추가했다. `Paused → Cancelled` 자동 호출과 실제 Claude Implementation adapter 연결, Bash 등 검증 명령 실행, IPC/UI, Codex, session/execution table은 포함하지 않는다.
- Unit 4c-4의 구현: `chatoms-ports::implementation`이 `ClaudeImplementationExecutor` port(`ImplementationExecutionBrief`/`ImplementationExecutionResult`/`ImplementationExecutionStartOutcome`)를 `chatoms-ports::planning::ClaudePlanningExecutor`와 동일한 모양으로 추가하며, `ImplementationExecutionResult`는 `TaskImplementationResultRecord`가 content 필드를 저장하지 않는 것과 일관되게 summary text를 포함하지 않는다. `chatoms-infrastructure::claude_implementation`의 `ClaudeImplementationAdapter`가 이 port를 구현하고, adapter 자신의 세 갈래 `ClaudeImplementationStartOutcome`(`Completed`/`PreflightRejected`/`StdinTooLarge`)을 port의 두 갈래 outcome으로 매핑한다(`StdinTooLarge`는 "spawn 전 실패"라는 점에서 `PreflightRejected`와 동일하게 취급되어 그쪽으로 합쳐짐). adapter 전용 `ImplementationResultOutcome`(Unit 4c-2가 정의한 로컬 타입)은 포트로 승격되지 않고, 대신 이 adapter 파일 안의 `map_outcome` 헬퍼가 이를 `chatoms_ports::repository::ImplementationResultOutcome`(Unit 4c-3)으로 변환한다 — Planning이 4b-2/4b-3에서 두 타입을 애초에 하나로 합쳤던 것과 의도적으로 다른 경로. `chatoms-application::implementation_execution`이 `PlanningExecutionStarter`/`PlanningExecutionRecorder`의 책임 분리를 그대로 따르는 `ImplementationExecutionStarter`/`ImplementationExecutionRecorder`를 추가한다. `ImplementationExecutionStarter::begin`은 Planning의 `begin`과 달리 fresh capability 확인 직후, `TaskService::start_implementation`을 호출해 `AwaitingDesignApproval → Implementing` 전이·동의 기록을 커밋하기 *전에* 필요한 증거(`WorktreeReady` isolation record의 worktree path, `Completed` outcome과 비어 있지 않은 plan text를 가진 저장된 Claude Planning 결과, `TaskBrief`)를 모두 읽기 전용으로 먼저 검증한다: 하나라도 없으면 subprocess를 시작하지 않는 것은 물론 동의도 기록하지 않고 상태도 전이하지 않은 채 fail-closed 에러만 반환한다(write 가능한 Implementation 실행은 부분적인 파일 변경을 되돌릴 안전한 방법이 없으므로, Planning의 "전이 먼저, 그 다음 실패 시 RecoveryRequired로 fallback" 순서를 그대로 재사용하지 않기로 결정함). `ImplementationExecutionRecorder::run_and_record`가 executor를 실행하고 그 결과를 `TaskService::record_implementation_result`로 전달하며, `PreflightRejected`나 executor 자체의 `Err`는 모두 `RecoveryRequired`로 fallback한다. `run_and_record_with_panic_containment`는 Planning의 `run_and_record_with_panic_containment`와 동일하게 `std::panic::catch_unwind`+`AssertUnwindSafe`로 executor 호출 전체를 감싸 panic을 잡고, payload를 검사·저장·전달하지 않은 채 동일한 `RecoveryRequired` fallback 경로를 재사용하며, 그 fallback 쓰기 자체가 실패하면 성공으로 위장하지 않고 오류를 그대로 반환한다. 이 Unit은 Tauri command, background thread, IPC client, ProjectsPage UI, cancel/resume 버튼, `Paused → Cancelled` 자동 전이, Bash 검증 명령, Testing/AutoFixing 실행, Codex, handoff/resume, session/execution table을 포함하지 않는다 — Planning이 4b-2(adapter)와 4b-4(IPC/UI 배선) 사이에 그랬던 것과 동일하게, IPC/UI 배선은 다음 Unit으로 미룬다.
- Unit 4c-5의 구현: `chatoms-application::provider_eligibility`의 `contract_for`가 `(Claude, Implementation)`을 `NotApproved`에서 `(Claude, Planning | Implementation | Review)` 그룹의 `Approved`로 승격했다 — 이 Unit이 실제로 Implementation 시작 UI를 노출하는 Unit이므로, Planning·Review가 이미 그랬던 것과 동일하게 이 시점에 계약을 승인 처리했다(`state_allows_work_kind`는 이미 Unit 2부터 `AwaitingDesignApproval`로 올바르게 게이팅되어 있었음). `src-tauri/src/state.rs`가 `PlanningRunRegistry`와 동일한 in-memory-only 모양의 `ImplementationRunRegistry`를 별도 타입으로 추가하고(공유 제네릭화 대신 work kind별 최소 이름·책임 유지), `AppRuntime`/`RuntimePorts`에 `implementation_runs` 필드로 배선했다. `src-tauri/src/commands/implementation.rs`가 `commands::planning`과 동일한 패턴으로 `handle_start_claude_implementation`(fresh capability 확인 → `ImplementationExecutionStarter::begin`으로 `AwaitingDesignApproval → Implementing` 동기 커밋 → registry 등록 실패 시 `RecoveryRequired` fallback → detached background thread에서 `ClaudeImplementationAdapter` 생성과 `ImplementationExecutionRecorder::run_and_record_with_panic_containment` 실행)와 `handle_cancel_claude_implementation`(registry에서 cancellation 요청, 상태는 직접 바꾸지 않음)을 추가했다. `UnregisterOnDrop` RAII guard가 `commands::planning`의 panic-safe unregister 패턴을 그대로 재사용한다. `start_claude_implementation`/`cancel_claude_implementation` Tauri command 2개가 `REGISTERED_HANDLERS`(22→24)와 `generate_handler!`에 추가됐다. `CancelImplementationDto`(`{requested: bool}`)를 `CancelPlanningDto`와 별도 타입으로 추가했다. 프런트엔드: `src/ipc/{types.ts,client.ts}`에 대응하는 타입·IPC_COMMANDS·`IpcClient` 메서드·`isCancelImplementationDto` 런타임 가드를 추가했다. `ProjectsPage`가 `awaitingDesignApproval`에서 `(implementation, claude)` eligibility로 게이팅된 "Start Claude Implementation" 버튼(및 Implementation 전용 문구의 동의 확인 화면 — `consentDialog`에 `workKind` 판별자 추가)을, `implementing`에서 진행 문구와 취소 버튼(기존 `planning` 상태와 동일한 2초 polling에 편입)을, `paused`/`testing`/`recoveryRequired`에는 각각 안전한 안내 문구만 표시한다. `Paused → Cancelled` 자동 호출, resume UI, 결과 상세 UI, provider 선택 UI는 추가하지 않았다.
- Unit 4c-5는 Bash 검증 명령 실행, `Testing`/`AutoFixing` 실행, `Paused → Cancelled` UI/API, resume, Codex, Claude write 재시도, provider 자동 전환, handoff, session/execution table을 포함하지 않는다.
- Unit 4d-1의 구현: `chatoms-domain`에 provider-neutral `ValidationCommandKind`(Format/Lint/Typecheck/Test/Build) vocabulary를 추가했다. `chatoms-ports::validation::ValidationCommandDiscovery` port와 `chatoms-infrastructure::validation_discovery::ManifestValidationCommandDiscovery` adapter가 task worktree의 `Cargo.toml`(존재 여부만 확인하는 고정 non-mutating cargo 서브커맨드: `fmt --check`/`clippy`/`test`/`build`)과 `package.json`(`scripts`의 5개 관례적 키 이름 존재 여부만 확인하고 값은 절대 읽지 않으며, `pnpm-lock.yaml`/`yarn.lock` 존재로 package manager를 선택해 `<manager> run <script>` 형태로만 후보를 만든다)을 읽기 전용으로 검사해 구조화된 executable+argv 후보만 제안하고, 어떤 프로세스도 실행하지 않으며 shell 문자열을 만들거나 파싱하지 않는다. `crates/chatoms-infrastructure/migrations/0010_task_validation_command_approvals.sql`이 `(task_id, approved_task_version, command_kind)` 복합 PK를 가진 immutable append 전용 테이블을 추가한다(Planning/Implementation 결과 테이블의 task당 1행 패턴과 달리, Testing은 `AutoFixing`/`ReviewFixing`을 거쳐 여러 번 재진입하므로 이 Unit부터 command kind별 여러 행을 허용하는 형태로 설계됨). `chatoms-application::validation_commands::ValidationCommandService`가 `list_candidates`(읽기 전용)와 `approve_command`(task가 `Implementing` 또는 `Testing`이고 version이 일치하며, 요청된 `(kind, executable, arguments)`가 지금 discovery가 제안하는 후보와 정확히 일치할 때만 승인 행을 원자적으로 저장)를 제공한다. SQLite 구현은 삽입 직전 트랜잭션 안에서 task의 현재 version과 상태를 다시 검증하고, executable/argument 문자열이 절대경로이거나 `..` 경로 순회를 포함하면 defense-in-depth로 거부한다. 이 Unit은 어떤 검증 명령도 실행하지 않고 Task 상태를 전이하지 않으며, `SharedFoundationRepository`(chatoms-infrastructure)와 `RepositoryHandle`(src-tauri) 양쪽 wrapper에 위임을 추가하고 실제 SQLite wrapper-chain 승인 저장·조회 테스트를 추가했다.
- Unit 4d-1은 검증 명령 실제 실행, 실행 결과 저장 table, `Testing`/`AutoFixing` 상태 전이, IPC/UI, Bash allowlist, Claude/Codex 자동 수정, Codex, resume/handoff, session/execution table을 포함하지 않는다.
- Unit 4d-1b의 구현: `docs/DECISIONS.md`의 "Validation tool executable trust" 결정에 따라, 승인된 검증 명령의 `executable`(discovery가 제안한 논리적 이름, 예: `cargo`)을 사용자가 지정한 하나의 절대경로와 그 파일의 Windows stable NTFS object identity(volume serial + file ID)에 결합해 저장한다. `chatoms_ports::filesystem::FilesystemIdentityPort`에 `inspect_supported_file` 메서드를 추가했다(기본값은 `Unsupported`; Windows 구현은 `chatoms-platform`의 기존 `open_file`/`identity_from_handle` private 함수를 그대로 재사용해 새 unsafe/FFI를 추가하지 않았다). `task_validation_command_approvals`(Unit 4d-1)를 별도 1:1 binding table로 분리하지 않고, forward migration `0011_validation_command_executable_binding.sql`로 그 자리에서 rebuild-and-widen했다(6개 NOT NULL 컬럼 추가: `approved_executable_path`, `executable_volume_serial_hex`, `executable_file_id_hex`, `tool_directory_path`, `tool_directory_volume_serial_hex`, `tool_directory_file_id_hex` — hex 컬럼은 `0002_git_isolation.sql`의 `root_volume_serial_hex`/`root_file_id_hex`와 동일한 길이·GLOB 제약을 재사용). 이 Unit 이전에는 어떤 UI/IPC도 이 테이블에 쓰지 않았으므로, widening 이전에 존재할 수 있는 행은 실제 사용자 trust 상태가 아닌 dev/test 잔재로 판단해 위조된 identity로 채우는 대신 삭제했다. 승인 record와 executable binding은 여전히 한 행이므로 "승인 없는 binding"은 구조적으로 불가능하다. `chatoms-application::validation_commands::ValidationCommandService`에 `F: FilesystemIdentityPort` 의존성을 추가했다: `approve_command`는 이제 discovery 후보 일치 확인 뒤 사용자 지정 절대경로가 절대경로인지, task worktree 밖인지(기존 `inspect_supported_directory`로 얻은 worktree canonical path와 component 단위 `starts_with` 비교) 검증하고 executable과 그 상위 tool directory의 identity를 캡처해 저장한다. 새 읽기 전용 `verify_binding` use case가 저장된 binding을 다시 조회해 현재 identity와 비교하고 `Verified`/`IdentityMismatch`/`NotFound`만 반환하며, 어떤 경로로도 재승인을 자동으로 대체하지 않는다. Authenticode signer 검증은 필수 gate로 추가하지 않았다(Git/Claude보다 약한 trust 모델임을 DECISIONS.md에 명시).
- Unit 4d-1b는 검증 명령 실제 실행, PATH 탐색, signer 필수 gate, package-manager script 본문 읽기, IPC/UI, `Testing`/`AutoFixing` 상태 전이, network sandbox·package install, Codex, resume/handoff, session/execution table을 포함하지 않는다.
- Unit 4d-2a의 구현: `chatoms_ports::process::ProcessSpec`에 `environment: Option<Vec<(OsString, OsString)>>` 필드를 추가했다(기존 Claude Planning/Implementation 호출부는 모두 `None`으로 이전 동작을 유지). `StdProcessRunner::run_streaming`은 `Some(vars)`일 때 `env_clear()` 후 `vars`만 설정하고, `None`일 때는 기존 `CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD` 제거 동작을 그대로 유지한다. `chatoms_ports::validation_execution`이 provider/tool-neutral `ValidationCommandExecutor` port와 `ValidationExecutionOutcome`(`Success`/`ExitFailure`/`TimedOut`/`StdoutBoundExceeded`/`Cancelled`/`Uncertain`)·`ValidationBindingRejection`(`IdentityMismatch`/`ExecutableInsideWorktree`/`UnapprovedCommandKind`) 결과 타입을 추가한다. `crates/chatoms-infrastructure/src/validation_execution.rs`의 `CargoValidationAdapter`가 이 port의 첫 구현체로, Cargo 4개 고정 서브커맨드만 실행한다: spawn 직전 매번 승인된 executable·tool directory와(구성된 경우) `CARGO_HOME`/`RUSTUP_HOME`의 identity를 `FilesystemIdentityPort`로 재검증하고, `approval.executable`/`arguments`가 이 adapter 자신의 고정 vocabulary(`expected_cargo_arguments`)와 정확히 일치하는지도 재확인한다. 환경은 `env_clear` + `PATH`(승인된 tool directory만)·앱 소유 `TEMP`/`TMP`·현재 프로세스의 `SystemRoot`·승인된 `CARGO_HOME`/`RUSTUP_HOME`(있는 경우)만 구성한다. `Format`/`Lint`/`Typecheck`는 10분, `Test`/`Build`는 30분 timeout을 Claude Implementation과 동일한 `DeadlineCancellationSignal` 합성 방식으로 적용하고, stdout은 2MiB로 bound하며 `DiscardingObserver`가 원문을 아무 곳에도 전달하지 않는다. `run_streaming`이 확인된 `Cancelled`를 반환했지만 호출자가 넘긴 원래 cancellation 신호는 flip되지 않았다면 이를 deadline 만료로 판단해 `TimedOut`으로 분류한다(포트 변경 없이 adapter 내부에서만 판별). `CARGO_HOME`/`RUSTUP_HOME` binding은 이 Unit이 저장하지 않으며, adapter 생성 시 호출자가 주입하는 `CargoEnvironmentBindings`(`chatoms_ports::filesystem::DirectoryIdentity` 재사용)로만 존재한다 — 저장·승인 UI는 이후 Unit의 책임이다.
- Unit 4d-2a는 결과 저장 migration/table, `Testing`/`AutoFixing`/`Reviewing`/`RecoveryRequired`/`Paused` 상태 전이, IPC/UI, package-manager script 실행, `CARGO_HOME`/`RUSTUP_HOME` 승인·영속화, network sandbox·package install, Codex, Claude Bash allowlist, resume/handoff, session/execution table, dependency/manifest/lockfile 변경을 포함하지 않는다.
- Unit 4d-2b의 구현: `crates/chatoms-infrastructure/migrations/0013_task_validation_command_results.sql`이 Cargo-only validation 실행 결과를 위한 append-only immutable 테이블 `task_validation_command_results`를 추가한다(기존 migration은 수정하지 않음). Testing은 `AutoFixing`/`ReviewFixing`을 거쳐 여러 번 재진입하므로 `task_planning_results`/`task_implementation_results`(task당 1행)와 달리 이 테이블은 `(task_id, approved_task_version, command_kind)`당 여러 행을 허용하며, `attempt_sequence`(1부터 시작)가 그 안에서 순서를 매긴다. `(task_id, approved_task_version, command_kind)`는 `task_validation_command_approvals`의 승인 식별자에 FK로 결합되고, `PRIMARY KEY(task_id, approved_task_version, command_kind, attempt_sequence)`이며, sequence 연속성은 trigger로도 재검증한다. `outcome`은 `ValidationExecutionOutcome`과 동일한 6가지 vocabulary(`Success`/`ExitFailure`/`TimedOut`/`StdoutBoundExceeded`/`Cancelled`/`Uncertain`)를 쓰고, `exit_code`는 `Success`/`ExitFailure`일 때만 허용된다. `safe_summary`는 빈 값이 될 수 없고 2000자 상한을 가지며, raw stdout/stderr·provider·session·transcript·executable path·environment path는 어떤 컬럼에도 저장하지 않는다 — 이는 이후 orchestration Unit이 `SecretRedactor`와 자체 크기 상한을 거쳐 만든 안전 요약만 전달해야 한다는 계약이다. update/delete는 trigger로 차단해 불변을 보장한다. `chatoms_ports::repository`에 `ValidationCommandResultOutcome`/`ValidationCommandResultAttempt`/`ValidationCommandResultRecord`와 `FoundationRepository::append_validation_command_result`/`list_validation_command_results` 최소 메서드를 추가했다. `append_validation_command_result`는 `IMMEDIATE` transaction 안에서 해당 approval이 실제로 존재하는지 확인하고 다음 `attempt_sequence`를 원자적으로 산출해 삽입하며, 호출자는 sequence를 알거나 추측할 필요가 없다. Task 버전이나 상태 검증·전이는 이 Unit의 책임이 아니므로 추가하지 않았다. `SharedFoundationRepository`(chatoms-infrastructure)와 `RepositoryHandle`(src-tauri) 양쪽 wrapper에 위임을 추가하고, 실제 SQLite wrapper-chain을 통한 append·list round-trip 테스트를 추가했다.
- Unit 4d-2b는 `CargoValidationAdapter` 실제 호출, `Testing`/`AutoFixing`/`Reviewing`/`RecoveryRequired` 상태 전이, Testing orchestration, background thread, cancellation registry, startup reconciliation, IPC/UI, package-manager script, Claude/Codex 실행, stdout/stderr 원문 저장·표시·DTO 노출, dependency/manifest/lockfile 변경을 포함하지 않는다.
- Unit 4d-2c의 구현: `crates/chatoms-application/src/testing_execution.rs`가 `TestingBatchStarter`/`TestingBatchRecorder`를 추가해 `CargoValidationAdapter`(`ValidationCommandExecutor`)를 Testing backend orchestration에 연결한다. `TestingBatchStarter::begin`은 task가 `Testing` 상태이고 expected version이 일치하는지, `WorktreeReady` isolation record가 있는지, 현재 task version에 승인된 검증 명령이 최소 1개 있는지 읽기 전용으로 검증하고, `ValidationCommandKind::ALL` 고정 순서(`Format → Lint → Typecheck → Test → Build`)로 정렬된 승인 목록을 반환한다 — approval이 하나도 없으면 상태를 바꾸지 않는 typed 오류이며, 이는 향후 승인 UI가 추가된 뒤 같은 task에서 재시도 가능해야 한다는 계약을 지키기 위함이다(Planning/Implementation Starter의 "정지 조건 실패 시 아무것도 쓰지 않는다" 계약과 동일). `TestingBatchRecorder::run_and_record`는 정렬된 승인을 순서대로 실행해, identity/argv binding이 거부되면(`ValidationExecutionStartOutcome::BindingRejected`) 해당 명령을 spawn하지 않고 상태 변경 없이 typed 오류를 반환하며, 승인된 마지막 명령이 성공하기 전까지의 모든 중간 `Success` 결과는 `FoundationRepository::append_validation_command_result`로 상태 변경 없이 append-only 기록한다. 배치를 끝내는 결과(마지막 명령의 성공, 또는 위치에 상관없이 처음 발생하는 `ExitFailure`/`TimedOut`/`StdoutBoundExceeded`/`Uncertain`/확인된 `Cancelled`)는 새 `FoundationRepository::finalize_validation_command_batch`(및 이를 감싸는 `TaskService::finalize_validation_command_batch`)로 결과 행과 `Testing → Reviewing`(전체 성공)/`Testing → Paused`(확인된 취소, `Task::pause()`로 resume target을 자동으로 `Testing`으로 설정)/`Testing → RecoveryRequired`(그 외 모든 outcome) 상태 전이·history 기록을 하나의 SQLite transaction으로 처리한다. `safe_summary`는 adapter가 raw stdout/stderr를 애초에 전달하지 않으므로 outcome별 고정 안전 문구만 사용한다. `run_and_record_with_panic_containment`는 (전체 배치가 아니라) 각 `start_validation_command` 호출 하나하나를 개별적으로 `catch_unwind`+`AssertUnwindSafe`로 감싸 panic을 정확히 어떤 command kind에서 발생했는지 알 수 있게 하고(`task_validation_command_results`가 task당 1행이 아니므로 Planning/Implementation의 전체-배치 wrapping 패턴을 그대로 재사용할 수 없었음), panic을 `Uncertain` outcome과 동일하게 취급해 같은 `finalize_validation_command_batch` 경로로 `RecoveryRequired`를 기록 시도한다. panic payload는 검사·문자열화·저장·DTO 전달을 하지 않는다. `finalize_validation_command_batch`의 원자적 쓰기 자체가 실패하면 `record_implementation_result`와 동일한 패턴(`recover_after_*_persistence_failure`)으로 결과 행 없이 `Testing → RecoveryRequired`로 fallback을 시도하고, 그 fallback마저 실패하면 오류를 그대로 전파한다.
- Unit 4d-2c는 Tauri command, background thread, IPC/UI, 승인 UI, startup reconciliation, cancellation registry, package-manager script, `AutoFixing`, Claude/Codex 실행, raw stdout/stderr 전달·저장·표시, 실제 Cargo/Claude/Codex CLI 실행, migration 추가(꼭 필요한 원자적 repository API 외)를 포함하지 않는다.
- Unit 4d-2d의 구현: Cargo-only Testing batch의 실행 수명주기를 Planning/Implementation과 동일한 모양으로 Tauri backend에 연결한다. `src-tauri/src/state.rs`의 `TestingRunRegistry`가 `PlanningRunRegistry`/`ImplementationRunRegistry`와 동일한 in-memory-only 모양(task id 중복 등록 거부, unregister, cancellation request)으로 별도 타입 추가됐고, `AppRuntime`/`RuntimePorts`에 `testing_runs` 필드로 배선됐다. `AppRuntime::app_temp_dir()`이 `RuntimeResources.paths`(앱 시작 시 해석된 `ResolvedAppPaths`)의 `temp_dir`을 읽어 `CargoValidationAdapter`가 필요로 하는 app-owned TEMP/TMP를 제공한다. `src-tauri/src/commands/testing.rs`의 `handle_start_validation_testing`이 `AppRuntime::app_temp_dir()`(없으면 `Unsupported` fail-closed)과 `TestingBatchStarter::begin`(task state/version, `WorktreeReady` isolation, 현재 version에 승인된 검증 명령 존재 여부를 읽기 전용으로 검증)을 확인한 뒤, `TestingRunRegistry`에 등록하고 detached background thread에서 `CargoValidationAdapter`와 `TestingBatchRecorder::run_and_record_with_panic_containment`를 실행한다. Planning/Implementation과 달리 `TestingBatchStarter::begin`은 상태 전이를 커밋하지 않으므로(task가 이미 `Testing`), registry 등록이 충돌하면(중복 등록) `RecoveryRequired` fallback 대신 상태를 그대로 둔 채 typed 오류만 반환한다 — 되돌릴 전이가 애초에 없기 때문이다. `handle_cancel_validation_testing`은 registry에 취소만 요청하고 상태를 직접 바꾸지 않으며, 확인된 취소만 기존 `TestingBatchRecorder`/`TaskService::finalize_validation_command_batch` 경로(Unit 4d-2c)를 통해 `Testing → Paused`(resume target `Testing`)로 기록된다. background thread는 `commands::planning`/`commands::implementation`과 동일한 `UnregisterOnDrop` RAII guard를 진입 직후 생성해 정상 종료·panic 어느 경로에서도 registry entry가 제거되도록 한다. `TaskService::reconcile_startup_testing`(`crates/chatoms-application/src/tasks.rs`)이 `reconcile_startup_planning`과 동일하게, 활성 lease의 task가 `Testing`이면 기존 `mark_recovery_required` 경로로 `RecoveryRequired`로 전이하고(non-terminal이므로 lease 유지), 활성 task가 없거나 `Testing`이 아니면 no-op이며, repository 오류는 그대로 전파한다. `src-tauri/src/bootstrap.rs`의 `compose_runtime`이 기존 `reconcile_startup_planning` 직후 이를 호출하며, 실패하면 동일하게 `ManagedRuntime::unavailable`로 fail-closed한다. `start_validation_testing`/`cancel_validation_testing` Tauri command 2개가 `REGISTERED_HANDLERS`(24→26)와 `generate_handler!`에 추가됐다. 새 `CancelTestingDto`(`{requested: bool}`)를 기존 `CancelPlanningDto`/`CancelImplementationDto`와 동일한 모양으로 추가했다. approval 검증·outcome 분류·panic containment·상태 매핑 자체의 정확성은 이미 Unit 4d-2a/4d-2b/4d-2c에서 충분히 테스트됐으므로, 이 Unit의 테스트는 command-layer 배선(registry 등록/거부/중복, cancel found/not-found, precondition 실패 시 등록·전이 없음)과 startup reconciliation에 집중한다.
- Unit 4d-2d는 validation candidate/approval Tauri command와 UI, ProjectsPage 수정, package-manager script, `AutoFixing`, raw stdout/stderr·safe summary UI 노출, resume/handoff, session/execution table, migration, dependency/manifest/lockfile 변경, 실제 Cargo/Claude/Codex CLI 실행을 포함하지 않는다.
- Unit 4d-2e의 구현: Testing 상태에서 Cargo validation candidate 확인·사용자 승인·승인된 batch 시작/취소 UI를 배선한다. `src-tauri/src/commands/validation_commands.rs`의 `get_validation_command_candidates`/`get_validation_command_approval_status`/`approve_validation_command` Tauri command 3개가 `REGISTERED_HANDLERS`(26→29)와 `generate_handler!`에 추가됐다. Candidate 조회는 기존 `ValidationCommandService::list_candidates`를 그대로 호출한 뒤 `executable == "cargo"`인 후보만 남겨 `kind`와 고정 안전 label만 담은 DTO로 축소하고, executable path·manifest 내용·package script·worktree path는 어떤 필드에도 담지 않는다. 승인 상태 조회는 `list_validation_command_approvals`의 결과에서 `kind`만 추출해 반환하며 stored executable/tool directory/`CARGO_HOME`/`RUSTUP_HOME` path나 identity는 절대 포함하지 않는다. 승인 IPC는 `kinds`(빈 배열·중복 거부)·`executablePath`(공백 거부)·optional `cargoHomePath`/`rustupHomePath`(공백이면 추측된 기본값 없이 `None`)만 입력으로 받고, 프런트가 보낸 executable/argv는 전혀 신뢰하지 않는다 — 선택된 kind마다 그 자리에서 다시 `list_candidates`를 호출해 현재 Cargo 후보의 `executable`/`arguments`를 재도출한 뒤 기존 `ValidationCommandService::approve_command`의 exact-candidate 검증에 그대로 넘기며, 어느 한 kind라도 현재 후보가 없으면 그 요청 전체를 아무것도 승인하지 않고 거부한다. 승인 성공 응답은 그 시점의 전체 승인된 kind 목록만 반환한다(입력 경로·identity는 응답에 없음). 이 과정에서 `src-tauri/src/state.rs`의 `FilesystemIdentityHandle`(모든 validation command 승인·재검증과 `CargoValidationAdapter`의 binding 재검증이 거치는 공유 wrapper)이 `inspect_supported_file`을 감싼 adapter에 위임하지 않고 트레이트 기본값(`Unsupported`)으로 항상 fail-closed되고 있던 기존 버그를 발견해 함께 수정했다(Unit 4b-4가 `RepositoryHandle`에서 겪었던 것과 동일한 모양의 위임 누락). 프런트엔드: `src/ipc/types.ts`에 `ValidationCommandKind`/`ValidationCommandCandidateDto`/`ValidationCommandApprovalStatusDto`/`ApproveValidationCommandInput`/`ApproveValidationCommandResultDto`/`CancelTestingDto`를, `src/ipc/validation_command.ts`에 정확한 key 집합만 허용하는 runtime guard를 추가했다. `ProjectsPage`의 `testing` 상태가 candidate/승인 상태 로딩 → Cargo executable 절대경로·optional `CARGO_HOME`/`RUSTUP_HOME` 입력 → 후보 선택 → "Approve selected validation commands"(자동 실행 없음) → "Start approved validation"(기존 `start_validation_testing`만 호출) → 진행 중 문구 + "Cancel validation"(기존 `cancel_validation_testing`만 호출) 흐름으로 교체됐다. 입력한 경로는 component state에만 있으며 승인 성공·실패·activeTaskId 변경 시 모두 초기화되어 다시 화면에 나타나지 않는다. 승인된 kind가 없으면 Start가 비활성화되고, 후보가 없으면 성공으로 간주하거나 상태를 전이하지 않는다. 기존 task polling(2초 간격)이 `testing` 상태에도 적용되도록 확장되어 `Paused`/`Reviewing`/`RecoveryRequired` 전이가 기존 안전 상태 문구로 반영된다(`reviewing`에 대한 고정 안전 문구도 이번에 추가). 실제 Cargo/Claude/Codex CLI는 어떤 테스트에서도 실행하지 않는다.
- Unit 4d-2e는 package-manager script 실행, `AutoFixing`, Claude/Codex 실행, raw stdout/stderr·저장된 safe summary 상세 표시, 승인된 executable/`CARGO_HOME`/`RUSTUP_HOME` 경로의 재조회·재노출, resume, `Paused → Cancelled` UI, session/execution table, migration, dependency/manifest/lockfile 변경을 포함하지 않는다.
- Unit 4e-1의 구현: Cargo-only validation batch 성공이 `TaskService::finalize_validation_command_batch`로 이미 자동으로 `Testing → Reviewing`을 커밋하므로(Unit 4d-2c), 사용자가 실제로 Claude Review를 시작할 수 있는 시점은 `Testing`이 아니라 `Reviewing`이다. `WorkKind::Review::entry_state()`와 `can_start_from()`(Unit 2 자리표시자)이 여전히 `TaskState::Testing`을 참조해 provider eligibility가 반대로 계산되던 결함을 `TaskState::Reviewing`으로 정정했다. `crates/chatoms-domain/tests/work_kind.rs`와 `crates/chatoms-application/tests/provider_eligibility.rs`에 `Testing`에서는 Review가 state mismatch로 startable이 아니고 `Reviewing`에서만 eligible/startable임을 검증하는 테스트를 추가·정정했다. Claude Planning/Implementation과 Codex `Unsupported` 동작은 변경하지 않았다. domain의 `can_transition_to`, `Reviewing`의 `ReviewFixing`/`AwaitingUserDiffApproval`/`Paused`/`Failed`/`RecoveryRequired` edge와 `(Claude, Review)` contract 승인은 이미 정의돼 있어 변경하지 않았다.
- Unit 4e-1은 Review consent 저장/`start_review` 유스케이스, Review 결과 table·상태 전이·startup reconciliation, `ClaudeReviewAdapter`, IPC/UI, diff 조회, provider capability/contract 정책 변경, 실제 Claude/Codex/Cargo 실행을 포함하지 않는다.
- Unit 4e-2의 구현: Claude Review 1회 provider 전송 동의를 `Reviewing` 상태에서 상태 전이 없이 기록·재사용하는 backend 기반을 추가했다. `crates/chatoms-infrastructure/migrations/0014_review_consents.sql`이 `task_provider_consents.work_kind` CHECK를 `Planning`/`Implementation`에서 `Planning`/`Implementation`/`Review`로 넓히는 forward-only 테이블 재생성 migration을 추가한다(0006/0008과 동일한 rebuild 패턴; 기존 migration은 수정하지 않고 기존 Planning/Implementation consent 행은 보존). `FoundationRepository::save_review_consent(expected_version, task_id, consented_at_ms) -> Result<ProviderConsent, RepositoryError>`(port 기본값은 여전히 `OperationFailed`)가 `SqliteFoundationRepository`에 구현되고, 하나의 `IMMEDIATE` transaction 안에서 task의 현재 version·상태(`Reviewing`)를 재검증한 뒤 기존 `(Claude, Review, expected_version)` 동의가 있으면 그대로 반환하고 없으면 새로 삽입해 반환한다 — task 상태·version·transition history·`ActiveTaskLease`는 이 메서드가 절대 건드리지 않는다. `SharedFoundationRepository`(chatoms-infrastructure)와 `RepositoryHandle`(src-tauri) 양쪽 wrapper에 위임을 추가하고 실제 SQLite wrapper-chain 왕복 테스트를 추가했다. `TaskService::start_review`(`StartReviewRequest{task_id, expected_version}`, actor/reason 없음 — 상태 전이가 없어 첨부할 transition history가 없음)가 task가 `Reviewing`이고 version이 일치하는지 확인한 뒤 `save_review_consent` 호출 결과를 그대로 `TaskView`로 반환한다. Planning/Implementation의 `start_planning`/`start_implementation`과 달리 이 use case는 어떤 `task.transition_to`도 호출하지 않는다(`Testing → Reviewing`은 이미 Unit 4d-2c에서 자동 커밋됨). Planning/Implementation/Review 동의는 work_kind가 복합 키의 일부이므로 완전히 독립적으로 저장·조회된다.
- Unit 4e-2는 실제 Claude/Codex/Cargo CLI 실행, diff 조회, `ClaudeReviewAdapter`, application execution starter/recorder·panic containment, Tauri IPC/UI, background thread/cancellation registry, Review 결과 table·상태 전이·startup reconciliation, provider capability/contract 변경, hardening flag 결정을 포함하지 않는다.
- Unit 4e-3의 구현: Claude Review 최종 결과를 immutable SQLite에 저장하고 결과·상태 전이·history를 하나의 transaction으로 처리하는 backend 기반을 추가했다. `crates/chatoms-infrastructure/migrations/0015_task_review_results.sql`이 `task_id` 1:1, `provider='Claude'`/`work_kind='Review'` 고정, outcome CHECK(`Completed`/`Failed`/`Cancelled`/`RecoveryRequired`), `review_text`는 `Completed`일 때만 1~100000자 존재 규칙을 가진 immutable 테이블을 추가한다(raw stdout/stderr, raw diff, transcript, tool I/O, prompt, executable/environment path, login/session/cost 정보는 어떤 컬럼에도 담지 않는다). `chatoms_ports::repository`에 `ReviewResultOutcome`(Planning과 동일하게 `Failed` 포함 — Review는 read-only라 확정적 실패에 외부 부작용 위험이 없음)과 `TaskReviewResultRecord`, `FoundationRepository::save_review_result`/`get_task_review_result`를 추가했다. `SqliteFoundationRepository::save_review_result`는 `save_planning_result`와 동일한 모양(`terminal: bool` 인자, task가 `Reviewing`인지 재검증, 결과 행 삽입·task 상태 갱신·transition history 삽입을 하나의 transaction으로 처리, `terminal`일 때만 `ActiveTaskLease` 삭제)을 재사용한다. `TaskService::record_review_result`가 outcome을 승인된 상태로 매핑한다: `Completed`→`Task::transition_to(AwaitingUserDiffApproval)`(findings 유무와 무관 — domain에 `Reviewing → Completed` edge가 없고 ReviewFixing 자동 라우팅은 이번 범위 밖), `Failed`→`Task::transition_to(Failed)`(유일한 terminal outcome), 확인된 `Cancelled`→`Task::pause()`(`resume_target_state = Reviewing`, Planning의 `Cancelled` 직접 전이 특례를 재사용하지 않음 — Reviewing은 STATE_MACHINE.md가 그 특례를 부여한 상태가 아니므로 다른 active-execution 상태와 동일하게 `Paused`를 거침), 그 외(`RecoveryRequired`)→`Task::transition_to(RecoveryRequired)`. 원자적 쓰기 자체가 실패하면 기존 `recover_after_planning_persistence_failure`와 동일한 패턴(`recover_after_review_persistence_failure`)으로 결과 행 없이 `Reviewing → RecoveryRequired`로 fallback을 시도하고, 그 fallback마저 실패하면 오류를 그대로 전파해 성공으로 추정하지 않는다(신규 `FakeRepository::fail_save_transition_once` 테스트 헬퍼로 이 이중 실패 경로를 검증). `SharedFoundationRepository`(chatoms-infrastructure)와 `RepositoryHandle`(src-tauri) 양쪽 wrapper에 위임을 추가하고 실제 SQLite wrapper-chain 저장·조회 테스트를 추가했다.
- Unit 4e-3은 `ClaudeReviewAdapter`, official CLI flag 확인, diff 조회, stdin/argv, startup reconciliation, panic containment, Tauri command/UI, background thread/cancellation registry, `ReviewFixing`, `AutoFixing`, package-manager script, dependency/manifest/lockfile 변경을 포함하지 않는다.
- Unit 4e-4a의 구현: 향후 Claude Review adapter의 stdin에만 일시적으로 실릴 task worktree의 현재 Git diff를 읽는 좁은 read-only capability를 추가했다. `chatoms_ports::diff`의 새 `WorktreeDiffPort` trait(`current_diff(worktree) -> WorktreeDiffOutcome`)을 기존 `GitService`에 범용 revision API로 추가하지 않고 별도 목적 전용 trait으로 분리했다. `chatoms-infrastructure::git`의 `GitCliAdapter`가 이 trait을 구현하며, `output`/`output_with_input`이 이미 쓰는 것과 동일한 trusted Git runtime·`env_clear`된 control-path 환경·고정 `common_arguments()`를 재사용하되(`GitService`의 다른 메서드와 동일한 신뢰 경계), unbounded `wait_with_output` 대신 이 Unit이 새로 추가한 `capture_bounded_stdout`(bounded stdout 읽기 + wall-clock timeout)로 실행한다. 고정 argv는 `-C <worktree> diff --no-color --no-ext-diff --no-textconv HEAD -- .`이며 어떤 revision·path도 호출자로부터 받지 않는다: `diff HEAD`(worktree 자신의 현재 커밋과 비교) 한 번 호출로 staged+unstaged 변경을 모두 포함하고, `--no-ext-diff`/`--no-textconv`가 external diff driver·textconv 실행을 차단한다. `DIFF_MAX_BYTES`(512KiB) 초과 시 buffer를 버리고 `DiffTooLarge`로, `DIFF_TIMEOUT`(20초) 초과 시 프로세스를 kill하고 `TimedOut`으로, 그 외 미확인 상태는 `Uncertain`으로 분류하며 이 셋은 모두 안전한 `Ok` outcome이다. Git 실행 실패(nonzero exit)와 non-UTF-8 stdout만 raw 내용 없는 typed `PortFailure`(`Conflict`/`InvalidInput`)로 반환한다. `chatoms-application::review_diff`의 `ReviewDiffReader`가 task가 `Reviewing` 상태·버전 일치인지, isolation이 `WorktreeReady`인지 확인한 뒤, Git 프로세스를 spawn하기 *전에* 기존 `GitService::verify_task_worktree`와 `FilesystemIdentityPort`(`inspect_supported_directory`+`verify_local_tree`)로 worktree identity를 재검증하고, 그 재검증을 통과했을 때만 `WorktreeDiffPort::current_diff`를 호출한다. 반환된 diff는 이 Unit의 어떤 경로로도 SQLite, DTO, IPC, 로그에 전달되지 않는다.
- Unit 4e-4a는 `ClaudeReviewAdapter`, Claude/Codex 실행, Review 결과 저장·상태 전이, Tauri IPC/UI, Git mutation(commit/checkout/reset/restore/stash/merge/rebase/push), package-manager script, `AutoFixing`, dependency/manifest/lockfile 변경을 포함하지 않는다.
- Unit 4e-4b의 구현: `chatoms_ports::review`의 새 `ClaudeReviewExecutor` port(`ReviewExecutionBrief`/`ReviewExecutionResult`/`ReviewExecutionStartOutcome`)가 Planning·Implementation의 동형 port를 그대로 미러링한다 — Review는 read-only이고 `TaskReviewResultRecord`가 content field(`review_text`)를 저장하므로 Implementation이 아니라 Planning 쪽 모양(content 보존)을 따른다. `chatoms-infrastructure::claude_review`의 `ClaudeReviewAdapter`가 이 port를 구현하며, argv는 Planning의 읽기 전용 조합(`-p --permission-mode plan --tools Read,Glob,Grep --add-dir <worktree> --output-format json`)에 Implementation이 확정한 hardening 3종(`--strict-mcp-config`, `--setting-sources project,local`, `--disable-slash-commands`)을 더하고 `--max-turns`만 8로 낮춘 것이다 — 이 조합의 모든 개별 flag는 이미 Planning/Implementation Unit에서 공식 문서 근거로 확인됐으므로 이번 Unit에서 새로 확인할 flag는 없다. `--bare`, `--dangerously-skip-permissions`, `--allowedTools`, `Edit`/`Write`/`Bash`, `--mcp-config` 값은 사용하지 않는다. TaskBrief 세 필드와 diff text는 고정 stdin 템플릿에만 실리며, diff 섹션 헤더와 argv의 고정 지시문 양쪽에서 diff를 "untrusted repository content"로 명시하고 그 안의 지시를 따르지 말라고 명시한다(Planning result는 이번 첫 입력에서 제외). stdin 총 크기는 spawn 전 `MAX_STDIN_BYTES`(1MiB — Unit 4e-4a의 diff 상한 512KiB에 TaskBrief·템플릿 여유분을 더한 값이며 CLI 자체의 10MiB 한도보다 훨씬 작음) 초과 시 truncation 없이 typed `ClaudeReviewStartOutcome::StdinTooLarge`로 fail-closed하고(port 레벨에서는 `PreflightRejected`로 합쳐짐, Implementation의 기존 fold 패턴과 동일), spawn 직전마다 `ProviderCapabilityPort`를 다시 호출해 미검증 캐시를 신뢰하지 않는다. stdout은 `MAX_STDOUT_BYTES`(2MiB, 기존 adapter들과 동일)로 bound된 내부 buffer에서만 처리되고 `--output-format json` envelope에서 `subtype`/`is_error`/`result`/`num_turns`만 파싱하며 session/cost 등 나머지 필드는 애초에 struct에 없어 파싱 대상이 아니다. 성공 결과 text는 `SecretRedactor::redact_text`를 거친 뒤에만 `ReviewResultOutcome::Completed`로 반환한다. Outcome 매핑은 이 Unit의 승인된 계약대로 nonzero exit·malformed/non-success JSON·stdout bound 초과를 모두 `Failed`로(Planning이 malformed 전용으로 `RecoveryRequired`를 쓰던 것과 의도적으로 다름 — Review도 Planning과 동일하게 `--tools Read,Glob,Grep`로 외부 효과가 구조적으로 불가능해 confirmed non-success에 "possibly in-progress" 함의가 필요 없다는 판단), 확인된 취소는 `Cancelled`, 미확인 종료(`StreamingOutcome::Uncertain`)만 `RecoveryRequired`로 분류한다. 이 Unit은 fake `ProviderCapabilityPort`/fake `StreamingProcessRunner`만으로 테스트하며 실제 Claude/Codex/Cargo CLI를 실행하지 않는다.
- Unit 4e-4b는 `task_review_results` 저장·상태 전이 연결, application starter/recorder, startup reconciliation, panic containment, Tauri command/UI, background thread/cancellation registry, Planning result 재전달, `ReviewFixing`, `AutoFixing`, Git diff port 변경, 실제 Claude/Codex/Cargo CLI 실행, dependency/manifest/lockfile 변경을 포함하지 않는다.
- Unit 4e-5의 구현: `ReviewDiffReader`(4e-4a), `TaskService::start_review`(4e-2), `ClaudeReviewExecutor`(4e-4b), `TaskService::record_review_result`(4e-3)를 Tauri/UI 없이 application orchestration으로 연결했다. `chatoms-application::review_execution`의 `ReviewExecutionStarter::begin`이 Planning의 "전이 먼저, 실패 시 RecoveryRequired로 fallback" 순서가 아니라 Implementation의 "모든 증거를 먼저 확인, 그 다음에만 커밋" 순서를 따른다 — Review 실행은 read-only이지만, 되돌릴 상태 전이가 없는 diff 검증 실패 뒤에 consent를 남기지 않기 위함이다: 프레시 capability 확인 → task 상태/버전과 `WorktreeReady` isolation·`TaskBrief`를 읽기 전용으로 확인 → `ReviewDiffReader::read_current_diff`로 재검증된 worktree의 bounded ephemeral diff를 먼저 읽고 `Diff` 변형만 통과시킴(`NoChanges`/`DiffTooLarge`/`TimedOut`/`Uncertain`은 새 `FailureCategory::Conflict` 매핑으로, identity mismatch·Git 실패·malformed 출력은 `ReviewDiffReader`가 이미 반환하는 typed 오류를 그대로 전파) → 이 모든 read-only 검증을 통과한 뒤에만 `TaskService::start_review`를 호출해 `(Claude, Review, expected_version)` consent를 저장·재사용한다(`start_review`는 4e-2의 "Reviewing 유지" 계약을 그대로 보존 — 상태 전이가 없으므로 diff 검증 실패는 애초에 되돌릴 필요가 없다). 성공 시 반환하는 `ReviewExecutionInputs`는 task·worktree path·`TaskBrief`·읽어온 diff text만 담고 저장된 Planning 결과는 포함하지 않으며(4e-4b가 이미 확정한 첫 입력 계약과 일치), `diff_text` 필드를 raw로 노출하는 `#[derive(Debug)]` 대신 byte 길이만 보이는 수동 `Debug` 구현을 둬 `chatoms_ports::diff::WorktreeDiff`와 동일한 보호를 이 타입에도 재현했다. `ReviewExecutionRecorder::run_and_record`는 `ClaudeReviewExecutor::start_review`를 호출하고 그 결과(어댑터의 `Completed`/`Failed`/확인된 `Cancelled`/`RecoveryRequired`)를 재해석 없이 그대로 `TaskService::record_review_result`에 전달한다 — outcome-to-state 매핑(`Completed→AwaitingUserDiffApproval`, `Failed→Failed`, `Cancelled→Paused(resume target Reviewing)`, 그 외→`RecoveryRequired`)은 이미 4e-3의 `apply_review_outcome`이 전부 구현하고 있어 이 Unit은 관여하지 않는다. `PreflightRejected`와 executor `Err`는 Planning/Implementation과 동일하게 `RecoveryRequired`로 fold된다. `run_and_record_with_panic_containment`는 Planning/Implementation과 동일한 `catch_unwind`+`AssertUnwindSafe` 패턴으로 executor 호출 전체를 감싸 panic payload를 검사·저장·전달하지 않고 동일한 `RecoveryRequired` fallback을 시도하며, 그 fallback 쓰기 자체가 실패하면 성공으로 위장하지 않고 오류를 그대로 반환한다(`crates/chatoms-application/tests/review_execution.rs`에 24개 테스트: 성공 경로, capability/상태/버전/isolation/brief 거부, diff `NoChanges`/`DiffTooLarge`/`TimedOut`/`Uncertain`/identity mismatch/포트 오류 거부, 동일 버전 consent 재사용, outcome 매핑 5종, panic containment와 fallback 실패 전파, `ReviewExecutionInputs`의 Debug 출력에 diff 내용이 노출되지 않음을 검증). 실제 Claude/Codex/Cargo/Git CLI는 어떤 테스트에서도 실행하지 않는다.
- Unit 4e-5는 Tauri command, background thread, cancellation registry, IPC/UI, startup reconciliation, Git diff port/adapter 변경, `ReviewFixing`, `AutoFixing`, package-manager script, resume/handoff, 실제 Claude/Codex/Cargo 실행, dependency/manifest/lockfile 변경을 포함하지 않는다.
- Unit 4e-6의 구현: Claude Review의 시작·취소 실행 수명주기, startup reconciliation, Tauri IPC, ProjectsPage 진행·결과 조회 UI를 연결했다(Planning이 4b-2→4b-4, Testing이 4d-2a→4d-2d/e로 그랬던 것과 동일하게 adapter/orchestration Unit 다음에 배선 Unit이 옴). `src-tauri/src/state.rs`의 `ReviewRunRegistry`가 `PlanningRunRegistry`/`ImplementationRunRegistry`/`TestingRunRegistry`와 동일한 in-memory-only 모양(task id 중복 등록 거부, unregister, cancellation request)으로 별도 타입 추가됐고, `AppRuntime`/`RuntimePorts`에 `review_runs` 필드로 배선됐다. `src-tauri/src/commands/review.rs`의 `handle_start_claude_review`가 Claude executable/preflight directory 게이팅(Planning/Implementation과 동일)을 먼저 통과시킨 뒤, 이 Unit이 처음 구성하는 두 개의 독립적인 fresh `GitCliAdapter` 인스턴스(`GitService` 역할 하나, `WorktreeDiffPort` 역할 하나 — 동일 객체에 대한 두 개의 동시 `&mut` borrow는 Rust에서 불가능하므로 `Clone`으로 분리)를 `ReviewExecutionStarter::begin`에 전달해 모든 read-only precondition(diff 포함)을 통과시킨 뒤에만 `(Claude, Review, expected_version)` consent를 커밋한다(Testing과 동일하게, `begin`이 상태 전이를 커밋하지 않으므로 registry 등록 충돌 시 `RecoveryRequired` fallback 없이 typed 오류만 반환 — 이미 저장됐을 수 있는 consent를 임의로 되돌리지 않음). 등록 성공 후 detached background thread에서 `ClaudeReviewAdapter`와 `ReviewExecutionRecorder::run_and_record_with_panic_containment`(4e-5)를 실행하고, `UnregisterOnDrop` RAII guard가 Planning/Implementation/Testing과 동일한 panic-safe unregister 패턴을 재사용한다. `cancel_claude_review`는 registry에 취소만 요청하고 상태를 직접 바꾸지 않으며, 확인된 취소만 4e-5의 기존 `Paused`(resume target `Reviewing`) 경로로 기록된다. `TaskService::reconcile_startup_reviewing`(`crates/chatoms-application/src/tasks.rs`)이 `reconcile_startup_planning`/`reconcile_startup_testing`과 동일하게 활성 lease의 task가 `Reviewing`이면 `RecoveryRequired`로 전이하고(non-terminal이므로 lease 유지), 그 외에는 no-op이며, `src-tauri/src/bootstrap.rs`의 `compose_runtime`이 기존 Testing reconciliation 직후 이를 호출하고 실패 시 동일하게 `ManagedRuntime::unavailable`로 fail-closed한다. `TaskService::get_review_result`(신규 `ReviewResultView`, `get_planning_result`/`PlanningResultView`의 순수 미러)와 `get_review_result` Tauri command(`ReviewResultDto`, `PlanningResultDto`와 동일한 패턴)가 현재 상태가 `AwaitingUserDiffApproval`일 때만 저장된 review_text/outcome/exit_code/turn_count/시각을 반환하고, 그 외 상태와 결과 미기록은 동일하게 `Ok(None)`으로 취급한다. `start_claude_review`/`cancel_claude_review`/`get_review_result` Tauri command 3개가 `REGISTERED_HANDLERS`(29→32)와 `generate_handler!`에 추가됐다. 프런트엔드: `src/ipc/{types.ts,client.ts}`에 대응하는 타입·IPC_COMMANDS·`IpcClient` 메서드를, `src/ipc/review_result.ts`에 `ReviewResultDto`/`CancelReviewDto`의 정확한 key 집합만 허용하는 runtime guard를 추가했다. `ProjectsPage`가 `reviewing`에서 `(review, claude)` eligibility로 게이팅된 "Start Claude Review" 버튼과 Review 전용 read-only 문구의 동의 확인 화면(`consentDialog.workKind`에 `"review"` 추가)을, 시작 후에는(상태 전이가 없으므로 로컬 `reviewRuns` 플래그로 추적) 고정 진행 문구와 "Cancel review" 버튼을, `awaitingUserDiffApproval`에서는 저장된 review 결과를 loading/empty/error 상태를 분리한 read-only 텍스트로 표시한다(diff 승인 버튼·ReviewFixing 버튼·결과 수정/삭제는 추가하지 않음). 기존 task polling(2초 간격)이 `reviewing`에도 적용되도록 확장됐다. 실제 Claude/Codex/Cargo/Git CLI는 어떤 테스트에서도 실행하지 않는다.
- Unit 4e-6은 최종 diff 승인/Completed 전이 버튼, `ReviewFixing`, `AutoFixing`, package-manager script, Review adapter/diff reader/result schema 변경, resume, `Paused → Cancelled` UI, Codex, Claude write 재시도, session/execution table, dependency/manifest/lockfile 변경을 포함하지 않는다.
- Unit 4e-8a의 구현: Phase 4 마감 감사에서 발견된 gap을 닫았다 — `Implementing`은 유일한 쓰기 권한 활성 상태인데도 `PlanningRunRegistry`/`TestingRunRegistry`/`ReviewRunRegistry`와 동일하게 메모리 전용인 `ImplementationRunRegistry`에 대응하는 startup reconciliation이 없어, 앱이 `Implementing` 도중 재시작되면 해당 task가 `ActiveTaskLease`를 쥔 채 영구히 멈추는 fail-open 상태였다. `TaskService::reconcile_startup_implementation`(`crates/chatoms-application/src/tasks.rs`)이 `reconcile_startup_planning`/`reconcile_startup_testing`/`reconcile_startup_reviewing`과 동일한 모양으로, 활성 lease의 task가 `Implementing`이면 기존 `mark_recovery_required` 경로로 `RecoveryRequired`로 전이하고(non-terminal이므로 lease 유지), 활성 task가 없거나 `Implementing`이 아니면 no-op이며, repository 오류는 그대로 전파해 성공으로 추정하지 않는다. `src-tauri/src/bootstrap.rs`의 `compose_runtime`이 `reconcile_startup_planning` 직후·`reconcile_startup_testing` 이전에 이를 호출하며(생성 순서상 Implementing이 Testing보다 먼저 오는 파이프라인 순서를 반영), 실패하면 기존 reconciliation들과 동일하게 `ManagedRuntime::unavailable`로 fail-closed한다. Implementation adapter/argv/consent/결과 저장/IPC/UI, registry/cancellation/resume, Planning hardening, Codex, migration, dependency/manifest/lockfile 변경은 포함하지 않는다.
- Unit 4e-8b의 구현: Phase 4 마감 감사에서 발견된 나머지 두 gap을 닫았다. 첫째, `crates/chatoms-infrastructure/src/claude_planning.rs`의 argv에 Implementation/Review가 이미 적용 중인 hardening 3종(`--strict-mcp-config`, `--setting-sources project,local`, `--disable-slash-commands`)을 `--max-turns` 뒤·고정 instruction 앞에 추가했다 — Planning의 `--tools Read,Glob,Grep` 자체는 그대로 유지되지만, `~/.claude/settings.json` hook은 tool 사용이 아니라 세션 이벤트에 반응하므로 그 allowlist로 애초에 gate되지 않았다는 것이 이번에 명시적으로 닫은 gap이다. 둘째, Implementation의 `MAX_STDIN_BYTES`(512KiB, plan_text 최대 100,000자를 포함)와 달리 Planning stdin은 plan_text 없이 TaskBrief 세 필드만 싣으므로 그보다 작은 `MAX_STDIN_BYTES = 256 * 1024`를 새로 추가하고, spawn 전 크기 검사로 초과 시 subprocess를 시작하지 않는 typed `ClaudePlanningStartOutcome::StdinTooLarge`를 반환한다. `chatoms_ports::planning::ClaudePlanningExecutor` 포트 구현이 이를 Implementation과 동일하게 `PreflightRejected`로 fold하며, 포트 자체의 두 갈래 outcome은 변경하지 않았다(`chatoms-application::planning_execution`은 이미 `PreflightRejected`를 일반적으로 `RecoveryRequired`로 처리하고 있어 추가 매핑이 필요 없었다). 셋째, `docs/SECURITY_POLICY.md`에 이미 존재하던 "Unit 4e-7" 서술(`approve_final_diff` command·`TaskService::approve_final_diff`·`AwaitingUserDiffApproval → Completed` 전이를 구현했다는 내용)을 제거했다 — 이 Unit이 설명하는 command/use case/전이는 `crates/`, `src-tauri/src/`, `src/` 어디에도 존재한 적이 없는 stale 문서였다. 해당 문단을 `docs/STATE_MACHINE.md`가 실제로 정의하는 `AwaitingUserDiffApproval`의 다음 상태(`Merging`/`Paused`/`Cancelled`, `Completed` 없음)와 일치하는 서술로 교체했다. Planning 결과/state/IPC/UI, Planning CWD/worktree/stdout parser/redaction 계약, Review/Implementation adapter, direct `AwaitingUserDiffApproval → Completed` edge나 승인 handler/UI 재도입은 포함하지 않는다.
- Phase 4 후속 기능을 선제 구현하지 않는다.
- Phase 2의 목적별 local Git 격리는 유지한다. remote Git, 기본 브랜치 mutation, branch/worktree 삭제·정리와 병합은 여전히 구현 범위 밖이다.
- 실제 provider session 시작과 Claude/Codex CLI 실행은 별도 요구사항 승인 없이는 구현하지 않는다.

## 아키텍처

- `domain` 계층은 Tauri, SQLite, CLI 프로토콜에 의존하지 않는다.
- UI는 Git, CLI, 파일 시스템을 직접 조작하지 않는다.
- 프런트엔드는 Tauri IPC를 통해서만 Rust application service를 호출한다.
- 공급자, Git, 프로세스, 업데이트 기능은 port와 adapter로 분리한다.

## 보안

- OAuth 토큰, API 키, 비밀번호, 쿠키, 인증 헤더, 개인키와 CLI 인증정보를 읽거나 출력하거나 저장하지 않는다.
- 로그와 artifact는 영구 저장 전에 민감정보를 마스킹한다.
- AI 출력은 비신뢰 입력으로 취급한다.
- 셸 문자열 직접 실행을 기본 실행 방식으로 사용하지 않는다.
- 프로젝트 밖 파일 변경과 외부 전송은 사용자 승인 없이는 금지한다.

## Git 및 작업 격리

- 향후 모든 코드 변경은 작업별 branch와 worktree에서 수행한다.
- 한 `Task`는 정확히 하나의 task branch와 최대 하나의 worktree만 가진다.
- 기본 브랜치를 직접 수정하지 않는다.
- 현재 저장소 자체의 최초 프로젝트 초기화 절차는 사용자의 명시적 지시에 따른다.

## 상태와 데이터

- `Task.project_id`는 생성 후 불변이다.
- 전체 앱의 단일 활성 작업은 `ActiveTaskLease`와 DB 제약으로 함께 보장한다.
- `Paused`, `RecoveryRequired`, `UnknownExternalEffect`를 포함한 모든 실행 비종료 상태는 `ActiveTaskLease`를 유지한다.
- `ActiveTaskLease`는 `Completed`, `Failed`, `Cancelled` 중 하나로 진입할 때만 해제한다.
- `CleanupPending`과 `Archived`는 종료 결과 이후의 보존 수명주기이며 lease를 다시 획득하지 않는다.
- 상태 변경은 임의 문자열 갱신이 아니라 Rust 도메인 유스케이스를 통해 수행한다.
- 현재 상태 갱신, 상태 전이 이력 저장과 종료 전이의 `ActiveTaskLease` 해제는 하나의 트랜잭션으로 처리한다.

## 코드 품질

- TypeScript strict 모드를 유지한다.
- Rust에서 불필요한 `unwrap`, `expect`, `panic`을 사용하지 않는다.
- 사용자 오류 메시지와 내부 오류 원인을 분리한다.
- 새 불변조건에는 테스트를 추가한다.
- migration은 재실행 가능해야 하며 적용 순서를 검증한다.
- 변경 후 Rust format, clippy, test와 TypeScript typecheck, 프런트 테스트를 실행한다.

## 패키지

- 새 패키지를 추가하기 전에 패키지명, 버전, 출처, 목적, 변경될 manifest와 lockfile을 사용자에게 보고한다.
- 전역 설치와 시스템 환경 변경을 금지한다.

## 작업 완료 보고

다음 순서로 보고한다.

1. 변경 요약
2. 생성·수정 파일
3. 실행한 명령
4. 테스트·빌드 결과
5. 실패 또는 미해결 위험
6. 다음 Phase로 넘긴 항목
