# ChatOMS 설계 결정

확정된 결정과 의도적으로 보류한 결정을 기록한다. 상세 근거와 요구사항은 [PRODUCT_REQUIREMENTS.md](PRODUCT_REQUIREMENTS.md), 구현 순서는 [PHASE_PLAN.md](PHASE_PLAN.md)를 참조한다.

| 결정 | 상태 | 내용 |
|---|---|---|
| 기술 스택 | 확정 | Tauri 2 + React + TypeScript strict + Rust + SQLite를 사용한다. |
| 플랫폼 | 확정 | Windows Native를 우선 지원하고 macOS를 지원한다. WSL은 MVP에서 제외한다. |
| Codex 제어 | 확정 | `codex app-server`를 제어 인터페이스로 사용한다. 공식 문서 기준 app-server의 maturity는 Experimental이며, schema/capability 검증 절차는 공식 문서 근거가 확인되고 별도 구현계획이 승인되기 전까지 구현하지 않는다. |
| Codex fallback | 확정 | app-server 호환 실패 시 `codex exec`로 자동 전환하지 않는다. |
| 작업 종류와 provider 분리 | 확정 | 설계·구현·리뷰는 작업 종류이며 특정 provider에 고정하지 않는다. 사용자가 각 작업 종류의 실행을 시작할 때 capability가 `Supported`이고 해당 작업 종류에 대한 승인된 실행 계약을 가진 eligible provider 중 하나를 선택한다. 장기적으로 Claude Code와 Codex 모두 모든 작업 종류에 선택 가능해야 하지만, 실제 선택 가능 여부는 provider별 capability 상태와 승인된 실행 계약에 따라 제한된다. 실행 중 provider 자동 전환, 세션 handoff와 Context Package 구조 변경은 현재 범위에 포함하지 않는다. |
| 모델 간 문맥 | 확정 | 전체 대화 대신 작업별로 versioned Context Package를 생성·마스킹·기록해 전달한다. |
| 공급자 외부 전송 | 확정 | 작업 시작 시 공급자와 데이터 범위를 한 번 승인하고 해당 작업의 후속 호출에만 재사용한다. |
| 격리 수준 | 확정 | ChatOMS application policy와 공식 CLI 권한·sandbox를 조합하며 OS 전체 수준의 완전 격리를 보장하지 않는다. |
| 변경 격리 | 확정 | 향후 모든 AI 코드 변경은 작업별 branch와 worktree에서 수행하고 원본 디렉터리는 기본적으로 읽기 전용으로 취급한다. |
| Phase 2 정리 범위 | 확정 | 자동 보상 삭제를 전면 금지한다. Git 생성 결과가 실패·불명확·사후 검증 실패이면 branch/worktree를 삭제하거나 재실행하지 않고 `RecoveryRequired`와 durable operation evidence를 기록한다. 보존·일반 수명주기 정리는 Phase 6 책임이다. |
| 프로젝트 root 등록 | 확정 | Git 하위 경로는 enclosing repository root를 후보로 표시하고 사용자 확인 후 등록한다. canonical 문자열은 표시 보조 정보이며 Windows volume serial과 directory file ID, repository kind와 Git common-dir identity를 mutation identity로 사용한다. Phase 2는 `DRIVE_FIXED` local directory만 지원하고 UNC, mapped·device network path, removable·RAM·CD-ROM·unknown drive, Cloud Files sync root·placeholder, linked worktree, separate git-dir, bare repository와 확인 불가능한 reparse root를 거부한다. |
| Phase 2 Git 시작 조건 | 확정 | 프로젝트 등록과 상태 조회는 dirty repository에도 허용하지만 task isolation은 tracked·untracked 변경이 없고 detached HEAD가 아니며 유효한 현재 branch와 하나 이상의 commit이 있을 때만 허용한다. |
| Git 초기화 승인 | 확정 | 비-Git 프로젝트의 `git init`과 initial snapshot은 project, task와 expected version에 결합된 명시적 승인 후에만 실행한다. 기존 `.gitignore`만 적용하고 자동 생성·수정하지 않으며, Git author 설정이 없으면 임의 작성자나 설정 변경 없이 중단한다. HEAD 검증 후에만 `GitInitialized`로 전이한다. |
| Worktree 관리 경로 | 확정 | task worktree는 `%LOCALAPPDATA%\ChatOMS\worktrees\<project-id>\<task-id>`에만 생성한다. 관리 parent와 hooks/template/config 경로의 stable identity를 mutation 직전에 재검증하며 target 선점이나 identity 변경 시 삭제 없이 실패한다. |
| Git 외부 효과 transaction | 확정 | SQLite와 Git 파일 시스템 효과를 단일 transaction으로 간주하지 않는다. intent와 command-start evidence 저장 → 구조화된 Git 실행 → 단계별 성공 receipt → 실제 상태 재검증 → 완료 확정 순서를 사용한다. `CompletionRecorded` receipt, task transition과 isolation summary는 하나의 SQLite transaction으로 기록하고 결과가 불명확하면 자동 추정·재실행·삭제 없이 `RecoveryRequired`를 기록한다. |
| Git adapter 경계 | 확정 | 의미 기반 `GitService` port만 공개한다. Windows production adapter는 PATH를 탐색하지 않고 HKLM Git for Windows installer record를 후보 discovery로만 사용한다. 64-bit view의 record가 있으면 그 candidate의 canonical path·runtime root·signer·ACL·identity 검증 실패를 32-bit view로 fallback하지 않고 fail-closed한다. 64-bit record가 없을 때만 32-bit record를 검사하며, 서로 다른 두 valid root는 ambiguity로 거부한다. candidate는 fixed-drive/final-path/stable identity, pinned Authenticode signer, runtime root·`cmd`/`mingw64/bin`·`libexec/git-core` distribution boundary를 통과해야 한다. certificate rotation 또는 runtime 교체는 fallback 없이 capability unavailable/fail-closed다. 모든 명령은 absolute executable, absolute `-C`, app-owned current directory, controlled PATH와 `GIT_EXEC_PATH`, `env_clear`를 사용한다. |
| Elevated Git capability | 확정 | ChatOMS는 asInvoker로 실행한다. TokenElevationTypeFull, TokenIsElevated != 0 또는 high-integrity 이상 token에서는 Git capability를 unavailable로 fail-closed 처리하며, linked limited token 사용이나 자동 self-de-elevation을 하지 않는다. token 조회 실패도 unavailable이며 사용자는 관리자 권한 없이 다시 실행해야 한다. |
| Git filter 정책 | 확정 | active `filter` attribute와 Git LFS, repository local `filter.*`, `include.*`, `includeIf.*`는 Phase 2에서 `UnsupportedRepository`다. 비-Git 프로젝트는 `git add` 전 모든 `.gitattributes`를 검사하고 stage 후 index 기준으로 재검사한다. 기존 Git 프로젝트는 base commit 계층과 `$GIT_DIR/info/attributes`를 함께 검사하며 info attributes가 변경되면 완료하지 않는다. 정적 repository의 filter 실행은 차단한다. 동일 사용자 권한의 악성 프로세스가 최종 검증과 Git 실행 사이를 의도적으로 맞추는 변경은 accepted residual risk이며, 이를 완전 차단한다고 주장하지 않는다. |
| Phase 2 재시작 복구 | 확정 | startup은 `GitInitInProgress`와 `WorktreeCreating` operation을 읽기 전용으로 재검증한다. durable 성공 receipt와 정확한 identity·Git 상태가 모두 일치할 때만 완료를 확정하며 그 외에는 `RecoveryRequired`로 전이한다. Git mutation·재실행·삭제와 lease 자동 해제는 하지 않고 lease/task 불일치는 corruption으로 fail-closed한다. |
| Legacy project migration | 확정 | 0001 project row는 0002 적용 전 OS stable identity preflight를 거친다. missing root, non-fixed/remote/cloud storage, identity 불명확 또는 stable identity 중복이면 migration을 rollback하고 project ID와 안전한 display path 진단만 제공한다. 자동 병합·삭제·비활성화는 하지 않는다. |
| Phase 2 프로젝트 경로 표시 | 확정 | canonical root path는 backend와 persistence에만 유지한다. 일반 `ProjectDto`에는 원본을 노출하지 않고 사용자 홈을 `%USERPROFILE%`로 치환한 display path만 제공하며 원본 경로는 로그·오류·telemetry에 포함하지 않는다. |
| 프로젝트 선택 UI | 확정 | Tauri native folder picker와 직접 입력·붙여넣기 수단을 함께 제공한다. `@tauri-apps/plugin-dialog`와 `tauri-plugin-dialog` 및 관련 manifest·lockfile 변경을 Phase 2에서 허용한다. |
| 병합 이력 | 확정 | 사용자 diff 승인 후 단일 작업 commit을 생성하고 기본 브랜치에 `--no-ff` 병합한다. |
| 작업 격리 cardinality | 확정 | 한 `Task`는 하나의 프로젝트, 정확히 하나의 task branch와 최대 하나의 worktree만 가진다. |
| 동시성과 lease | 확정 | 여러 프로젝트를 등록할 수 있지만 앱 전체의 활성 작업은 하나다. `Paused`, `RecoveryRequired`, `UnknownExternalEffect`를 포함한 모든 실행 비종료 상태가 `ActiveTaskLease`를 유지하고 `Completed`, `Failed`, `Cancelled` 진입 시에만 해제한다. |
| Migration 경계 | 확정 | 검증된 ChatOMS 내부 SQLite forward migration과 사용자 대상 프로젝트의 schema·migration·데이터 변환 정책을 분리하며, 후자는 별도 사용자 승인이 필요하다. |
| Claude 실행 계약 | 확정 | Planning과 Review는 `--permission-mode plan` 및 `Read,Glob,Grep` allowlist로, Implementation은 `default` mode에서 `Read,Glob,Grep,Edit,Write`와 `Edit,Write` 승인으로 실행한다. 세 경로 모두 trusted preflight CWD, `--add-dir` worktree, `--strict-mcp-config`, `--setting-sources project,local`, `--disable-slash-commands`, fresh executable trust·compatibility·login preflight를 적용한다. stdout/stderr 원문·session 정보는 UI, 로그, DB, artifact에 노출·저장하지 않으며 불확실한 write 결과는 `RecoveryRequired`로 fail-closed 처리한다. |
| Claude executable trust | 확정 | 프로필별 사용자 지정 절대경로만 사용하며 PATH 탐색과 고정 후보 경로 자동 스캔을 하지 않는다. Windows Authenticode signer `Anthropic, PBC`를 실행 직전마다 재검증하고, 서명을 확인할 수 없거나 검증에 실패하면 capability를 `Unavailable`로 fail-closed 처리한다. |
| Codex executable trust | 확정 | 서명 또는 동등한 신뢰 근거가 공식 문서로 확인되고 별도 구현계획이 승인되기 전까지 Codex capability는 `Unsupported`로 보고된다. 사용자 지정 경로에도 예외를 두지 않으며 `codex exec` 자동 fallback을 추가하지 않는다. |
| Validation tool executable trust | 확정 | Testing 검증 명령(cargo/node/npm/pnpm/yarn 등)의 executable은 Git·Claude보다 **약한 trust 모델**을 사용한다. PATH를 탐색하지 않고 task별·task version별로 사용자가 1회 승인한 절대경로만 저장·사용하며, 승인 시와 향후 실제 실행 직전 모두 Windows stable NTFS object identity(volume serial + file ID, 기존 `FilesystemIdentityPort`로 검증)를 재확인한다. Authenticode signer 검증은 필수 gate가 아니다 — rustup 배포 cargo/rustc, npm/pnpm/yarn shim 등 흔한 Windows 설치 방식이 Git for Windows installer나 Claude Code CLI처럼 단일 pin 가능한 signer로 안정적으로 서명되어 있다는 근거가 없기 때문이다. identity가 불일치하면 자동 복구 없이 새 승인을 요구하며, 승인된 executable은 task worktree 내부에 있을 수 없다. 향후 실제 실행 환경은 `env_clear` + 승인된 tool directory만 포함한 controlled PATH를 사용한다. |
| Scoped validation approval and post-merge provenance | 확정 | `ValidationExecutionScope`는 closed vocabulary `TaskWorktree | ProjectRoot`이며 approval identity는 `(task_id, approved_task_version, execution_scope, command_kind)`다. 기존 approval/result는 forward migration에서 `TaskWorktree`로 손실 없이 backfill하며, `TaskWorktree` approval은 `ProjectRoot` 실행에 재사용하지 않는다. `ProjectRoot` Cargo validation은 merge 전 `AwaitingUserDiffApproval`에서 diff approval과 별도의 immutable record로 승인하고 confirmed project identity revision 및 root stable identity snapshot에 결합한다. Post-merge starter는 임의의 최신 approval이나 version 산술을 사용하지 않고 immutable transition history의 `AwaitingUserDiffApproval -> Merging -> PostMergeTesting` chain에서 approval version을 복원한다. 실행 직전에는 live ProjectRoot identity, approval snapshot, executable/tool/environment binding을 다시 검증해야 한다. Post-merge 결과는 Testing 결과와 분리된 immutable table에 저장하고, 전체 성공만 `Completed` 및 lease 해제로 finalize한다. nonzero, timeout, output bound, binding rejection, cancellation, uncertainty, panic 또는 persistence uncertainty는 모두 `RecoveryRequired`로 finalize하고 lease를 유지한다. 상태 머신에 존재하는 `PostMergeTesting -> Failed` edge는 이번 Phase에서 사용하지 않는다. |
| Cargo-only validation 실행 범위 | 확정 | 승인된 Cargo 고정 서브커맨드(`fmt --all --check`/`clippy --workspace --all-targets --all-features`/`test --workspace`/`build --workspace`)만 실제 실행한다. executable·tool directory·선택적 `CARGO_HOME`/`RUSTUP_HOME`의 identity를 spawn 직전 재검증하고 `env_clear` 기반 환경·timeout·2MiB stdout bound를 적용한다. 결과는 마스킹·크기 제한된 안전 summary만 append-only로 저장하며 성공 배치는 `Reviewing`, 확인된 취소는 `Paused`, nonzero·timeout·output bound·uncertain은 `RecoveryRequired`로 전이한다. package-manager script, `AutoFixing`은 Phase 5 이후 범위다. `cargo test`/`cargo build`가 실행하는 worktree 코드의 네트워크·child process·Git mutation은 accepted residual risk다. |
| 프로필 연동 구현 시점 | 확정 | Phase 1은 `AppProfile`·`ProviderBinding` 모델만 정의한다. Phase 3은 Claude provider enablement를 우선하여 기본 profile에 귀속된 Claude 실행파일 절대경로 지정·저장과 Claude capability 새로고침만 구현하며 profile 선택 UI는 후속 Phase로 미룬다. 이는 역할 고정이 아니라 provider enablement 우선순위 결정이다. 프로필별 `CODEX_HOME`과 Claude 프로필 분리의 공식 지원 여부 검증은 이번 Phase 3에서 구현하지 않으며, 그때까지 Codex capability는 「Codex executable trust」에 따라 `Unsupported`로 보고된다. Claude 프로필 분리의 공식 지원을 확인하지 못하면 Claude는 장비별 단일 로그인을 사용한다. |
| 모델 추천 구현 시점 | 확정 | model recommendation은 application layer 정책 service로 두며, 추천 근거·비용 정보·사용자 override·프로젝트별 고정은 Phase 5로 이관한다. 승인 없는 고비용 모델 자동 상향은 금지한다. |
| 보존 기간 | 확정 | 완료·중단 작업 기록은 90일, 병합된 worktree와 작업 branch는 안전 조건 충족 시까지 최소 7일 보존한다. |
| 업데이트 배포 | Phase 7 전 결정 | 배포 채널, 릴리스 저장소, 서명키 보관 주체와 플랫폼별 rollback 방식을 Phase 7 시작 전에 확정한다. GitHub Releases와 CI 서명키는 후보일 뿐 현재 결정이 아니다. |
| Phase 1 build gate | 확정 | Node 22.18.0과 repository `packageManager`의 pnpm 11.9.0을 `corepack pnpm`으로 선택한다. Tauri before-build도 같은 Corepack entrypoint를 사용하고, Windows에서는 `node_modules\.bin\tauri.cmd`를 직접 실행해 debug `--no-bundle -- --offline --locked -j 1`로 검증한다. |
| Installer 검증 시점 | 확정 | MSI·NSIS와 WiX 공급망은 배포·업데이트 단계에서 검증한다. Tauri는 MSI 생성 시 사용자 cache에 WiX를 외부 다운로드할 수 있으므로 Phase 1에서는 bundle을 생성하지 않는다. Installer icon은 최종 제품 icon 확정 후 검증한다. |
| 정적 전이와 contextual 전이 | 확정 | 정적 상태 행렬은 문맥 없는 전이만 다룬다. 정상 업무 상태의 pause, `Paused`에서 저장된 target으로의 재개, `RecoveryRequired`의 target 설정·pause·재개는 검증 token과 Task aggregate API로 수행한다. `Paused`는 항상 resume target을 보유하며 transition sequence는 1부터 시작하고 연속성 검증은 repository가 담당한다. |
| 불명 외부 효과 복구 | 확정 | `UnknownExternalEffect`는 resume target을 보유하거나 `Paused`로 직접 전이하지 않는다. `RecoveryRequired`로 이동해 복구 분석과 target 검증을 마친 뒤에만 재개하며, `RecoveryRequired → Paused`는 검증된 resume target이 있을 때만 target을 유지하며 허용한다. |
| SQLite connection과 migration | 확정 | Production file DB는 connection마다 `foreign_keys=ON`, WAL, `synchronous=FULL`, 5초 busy timeout을 적용·재검증한다. Migration은 embedded forward-only SQL, 원문 SHA-256 checksum과 독립 transaction을 사용하며 commit 전 `foreign_key_check`를 수행한다. Lifecycle 자동 trigger와 checksum 자동 갱신은 금지하고 sequence 연속성은 repository가 검증한다. |
| Repository transaction 경계 | 확정 | Repository가 Task lifecycle transaction을 소유한다. 생성은 Task·최초 `Created` transition·lease를 함께 commit하고, 종료는 Task 갱신·transition·lease 삭제를 함께 commit한다. expected version과 transition sequence를 검증하며 active lease 자동 탈취·교체와 retry를 금지한다. Persistence row는 domain aggregate와 transition으로 검증해 복원한다. |
| Windows 앱 데이터 경로 | 확정 | `%LOCALAPPDATA%\ChatOMS` 아래에 `data`, `logs`, `artifacts`, `temp`, `worktrees`를 분리하고 DB는 `data\chatoms.sqlite3`를 사용한다. Identifier는 `io.github.minsangoh.chatoms`로 유지한다. |
| Windows 앱 데이터 ACL | 확정 | current user와 `NT AUTHORITY\SYSTEM`만 Full Control을 허용하는 protected DACL을 적용·재검증한다. Broad writable principal, reparse point 또는 비-Secure 결과에서는 영구 저장을 차단하며 관리자 권한을 요구하지 않는다. |
| macOS path·permission 검증 | 연기 | Phase 1은 port와 compile 가능한 stub 구조만 제공한다. 실제 Application Support 경로와 권한 적용·빌드 검증은 macOS 환경에서 별도로 수행한다. |
| Cross-layer failure category | 확정 | Platform-neutral `FailureCategory`, severity와 retry 계약은 `chatoms-ports`가 소유한다. Application은 이 계약만 stable error code로 변환하며 infrastructure/platform concrete error를 소유하거나 참조하지 않는다. |
| Source chain과 사용자 오류 분리 | 확정 | SQLite·I/O·OS·tracing source chain은 concrete adapter error에 유지한다. Application error는 category, severity, retry와 고정된 사용자 안전 메시지만 보유한다. |
| RedactedText 저장 경계 | 확정 | 비신뢰 텍스트는 bounded·deterministic redaction 및 잔여 민감정보 검증을 통과한 `RedactedText`로만 영구 진단 경계를 통과한다. 실패 시 raw fallback 없이 fail-closed marker를 사용한다. |
| 로컬 진단 로그 | 확정 | 권한 검증된 logs directory에 ANSI 없는 structured JSON을 non-blocking daily rolling file로 기록한다. Remote telemetry는 사용하지 않으며 application bootstrap 연결은 후속 단계에서 수행한다. |
| Application dependency inversion | 확정 | Application service는 domain과 ports만 사용하며 infrastructure/platform concrete adapter, filesystem, SQLite, process 또는 Tauri에 의존하지 않는다. |
| Bootstrap fail-fast 순서 | 확정 | Secure storage → database → logging → active lease 순서로 호출한다. Storage 또는 database가 준비되지 않으면 후속 port를 호출하지 않으며 logging failure는 raw fallback 없이 degraded health로 처리한다. |
| Application 시간 정책 | 확정 | Use case timestamp는 `TimeProvider` port에서 받는다. Application은 `SystemTime`이나 environment를 직접 읽지 않으며 production clock adapter는 후속 wiring 단계에서 제공한다. |
| Task identity 생성 책임 | 확정 | Task 생성 use case가 domain UUIDv7 ID를 만들고 canonical task branch identity와 최초 transition을 구성한다. 사용자 입력으로 task ID나 branch identity를 받지 않는다. |
| Contextual resume capability | 보류 | Pause와 `RecoveryRequired` 진입은 구현하지만 resume, recovery target 확정과 recovery pause는 실제 외부 상태 validation capability가 추가될 때까지 `Unsupported`다. Opaque validation token을 임의 생성하지 않는다. |
| Application read model | 확정 | `BootstrapStatus`, `SystemStatus`, `ProjectView`, `TaskView`, active task와 transition view는 Tauri/serde 독립 모델이다. IPC DTO와 사용자 경로 표시 정책은 IPC 단계에서 별도 결정한다. |
| Production adapter wiring | 확정 | Platform과 infrastructure의 production adapter 생성·수명 관리는 Tauri composition root 책임이다. Application은 ports만 사용하며 bootstrap은 startup에서 한 번 수행한다. |
| Startup critical/degraded 정책 | 확정 | Secure storage 또는 database 실패는 `Unavailable` runtime으로 제한하고 system health만 안전하게 제공한다. Logging 실패는 raw fallback 없이 `Degraded` ready로 처리하며 logging guard는 runtime 수명 동안 보존한다. |
| Phase 1 IPC surface | 확정 | System 4개, project 목록 1개, task read-only 3개 command만 등록한다. Task create·transition IPC는 Git/worktree/provider orchestration이 준비될 때까지 공개하지 않는다. |
| IPC DTO 및 경로 정책 | 확정 | Application read model과 serde DTO를 분리하고 camelCase를 사용한다. `ProjectDto`에는 전체 root path를 포함하지 않으며 IPC error에는 source, path, SQL, SID, stack 또는 secret을 포함하지 않는다. |
| Frontend IPC boundary | 확정 | Page는 `invoke`를 직접 호출하지 않고 중앙 typed client의 read-only API만 사용한다. Response는 `unknown`에서 DTO guard로 검증하고 오류는 승인된 code·message·severity·retry만 render한다. |
| Frontend state 관리 | 확정 | Phase 1 `/system`과 `/projects`는 React local component state를 사용한다. 별도 state management 또는 query/cache package를 추가하지 않는다. |
| Frontend mutation 범위 | 확정 | Phase 4는 TaskBrief 생성, provider consent, Claude Planning·Implementation·Review 시작·취소, Cargo validation 승인·시작·취소와 안전한 결과 조회 UI를 허용한다. 원시 provider 출력·diff·실행 경로는 노출하지 않는다. 고위험 승인, 다중 provider/model 선택, 일반 settings/profile 관리, Codex 설정 UI, profile 삭제·이름 변경·다중 profile 생성·관리는 Phase 5 이후 범위다. |
| Context Package v1 저장 방식 | 확정 | Context Package는 대안 B(immutable reference + content-free metadata만 영구 저장, provider 호출 직전에만 본문을 조립)를 따른다. 이 결정의 첫 구현 단계로 provider-transmission consent identity를 `(task_id, provider, work_kind, approved_task_version, data_scope)` 5-tuple로 확장했다. `data_scope`는 사용자 입력이나 AI 출력이 아니라 코드가 소유하는 `ContextDataScope`(`LegacyPhase4`/`ContextPackageV1`) 고정 enum이며, work_kind와 독립적인 다섯 번째 identity 축이다. 기존 Planning/Implementation/Review consent 행은 삭제·재해석 없이 `LegacyPhase4`로 backfill해 보존했다. `ContextPackageV1`은 저장 vocabulary로만 존재하며, Context Package manifest·조립기·payload 상한·outbound redaction·content hash는 후속 Unit(5a-2 이후)의 책임이다. |
| High-risk approval 범주 vocabulary | 확정 | `docs/PRODUCT_REQUIREMENTS.md` §9의 13개 고위험 설계 범주(아키텍처·DB schema·인증권한구조·보안정책 변경, 외부 네트워크·전송 추가, 대규모 파일 이동삭제, 공개 API·저장형식 변경, OS 설정 변경, 관리자 권한 필요, 호환성 파괴 변경, 데이터 마이그레이션, 복구 어려운 변경)를 하나도 생략·병합하지 않고 `chatoms_domain::HighRiskCategory` closed enum으로 도입했다. 이 vocabulary는 provider·work_kind와 무관한 별도 승인 축이며(provider consent·validation executable trust·diff 승인 어느 것과도 FK로 결합하지 않음), task version에 결합된 immutable 저장 경계(`task_high_risk_approvals`)만 확정·구현했다. 13개 범주를 실제로 자동 분류하거나, 이 분류를 근거로 사용자에게 승인을 자동 요청하거나, 승인 여부로 `AutoFixing`/`ReviewFixing`/`Merging` 실행을 게이팅하는 로직은 아직 구현되지 않았으며 후속 Unit의 책임이다. |
| Provider Implementation operation-risk declaration과 policy evaluation | 확정 | `AwaitingDesignApproval`은 모든 Implementation 전 정책 확인 지점이며, 위험 평가 완료 여부는 기존 category별 approval과 별도로 `(task_id, approved_task_version, ProviderImplementation)` immutable parent로 기록한다. parent만 있고 child가 없으면 명시적인 empty assessment, parent가 없으면 미평가다. non-empty child는 동일 task/version의 기존 `task_high_risk_approvals`에 FK로 결합하며 extra approval은 declaration 범위를 넓히지 않는다. declaration은 provider/model-neutral이고 confirmed ProjectRoot identity와 live app-created task worktree stable identity로 계산한 content-free SHA-256 digest에 결합한다. Policy Engine은 `ProviderImplementation`만 지원하며 task/state/version/lease, declaration과 exact approval, task/project/isolation 관계와 live identity digest를 authoritative source에서 다시 읽는다. declaration 부재만 `AssessmentRequired`, 정상 불일치는 content-free denial, repository·identity inspection·corruption 실패는 application error로 유지한다. 통과 시 발급하는 `PolicyPermit`은 task/version/operation/digest에 결합된 in-memory non-clone/non-debug/non-serializable capability이며 persistence·DTO·IPC 변환이 없다. Tauri/UI, Implementation starter/adapter/spawn gate, provider/model 선택, `AutoFixing`/`ReviewFixing`은 아직 열지 않는다. |
| Scoped local-user-only raw diff exception | 확정 | Unit 5c-1 감사가 확인한 충돌(§17/§22.5의 "파일별 diff 표시" 요구 vs. 이 코드베이스 전반의 "raw diff는 IPC/UI/영구저장에 노출하지 않는다" 관행)을 다음과 같이 해소한다: raw diff는 `AwaitingUserDiffApproval`의 user diff review 표면(`get_user_diff_for_review` command와 전용 `UserDiffReviewModal` 컴포넌트)에서만, 요청한 로컬 사용자 본인에게 일시적으로 표시할 수 있다. 이 raw diff는 DB, 파일 저장소, provider input, structured log, 재사용 가능한 IPC 캐시, `localStorage`/`sessionStorage`, 전역 store 어디에도 저장·전송하지 않으며 modal close·active task 변경·component unmount 시 즉시 폐기된다. 이 scoped exception은 이 표면에만 적용되며 Context Package, provider 결과, 다른 일반 task UI로 확장하지 않는다. 승인 자체는 `(task_id, approved_task_version, diff_content_hash)` — diff 텍스트가 아니라 그 SHA-256 content hash — 에만 결합하며, `task_diff_approvals`는 hash만 저장하고 raw diff는 저장하지 않는다. |
| User diff approval canonical candidate | 확정 | `AwaitingUserDiffApproval`의 approval hash는 향후 `git add -A -- .`가 포함할 task worktree 후보 전체, 즉 `HEAD` 대비 tracked staged+unstaged 변경과 ignored가 아닌 untracked regular UTF-8 files의 deterministic canonical text에 결합한다. ignored와 `.git` 내부는 포함하지 않으며, display text와 digest는 반드시 동일한 canonical bytes에서 함께 파생한다. candidate를 안전하게 canonicalize할 수 없으면 부분 표시/부분 hash 없이 fail-closed하고, approval 및 향후 merge 직전 같은 candidate를 다시 계산해 exact hash를 비교한다. raw candidate는 scoped local-user-only modal의 일시 메모리 밖에 저장·전송하지 않는다. |
| §18.2 Git diff 파일 저장 유예 | Phase 6 전 결정 | `docs/PRODUCT_REQUIREMENTS.md` §18.2가 "Git diff"를 파일 저장소 보관 항목으로 명시하지만, 이는 이번 세션까지의 "raw diff 비영구화" 관행과 직접 충돌한다. 이 결정은 Phase 6(기록·복구·보존) 시작 전까지 유예하며, 그때 마스킹·보존기간·접근권한 정책을 함께 확정하기 전에는 diff를 어떤 형태로도 영구 파일 저장소에 기록하지 않는다. |
| Manual merge-resolution confirmation identity | 확정 | `MergeConflict`의 수동 해결 확인은 diff approval·high-risk approval·provider consent와 별도인 네 번째 독립 승인 축이며 그 중 어느 것과도 FK로 결합하지 않는다. 식별자는 `(task_id, merge_conflict_task_version, resolution_digest_hex)`이고, digest는 `chatoms.manual-merge-resolution.v1` envelope — task id, project id, 현재 `MergeConflict` task version, source diff-approval task version, base/task branch, base/task/`MERGE_HEAD` commit, original checkout의 정렬된 stage-0 `(mode, object id, path)` 전체를 length-prefixed하여 SHA-256 — 로만 결합하며 raw path·file content는 digest 계산 범위 밖(adapter 로컬 메모리)에만 존재한다. 서로 다른 digest는 항상 새 immutable row이며 기존 row는 절대 갱신·삭제하지 않는다. `git write-tree`는 Git object DB를 쓸 수 있어 confirmation 계산에 사용하지 않는다. |
| Merge-continue write contract | 확정 | `MergeConflict → Merging`은 exact `ResolvedPendingConfirmation`급 candidate(`Ready`)와 exact immutable confirmation이 모두 확인된 뒤에만 커밋하며, 애플리케이션 preflight와 adapter prewrite gate가 동일한 identity·topology·residue(`MERGE_AUTOSTASH` 포함)·configuration·digest 검증을 독립적으로 재수행한다. 유일한 Git write는 원본 checkout에서 실행하는 고정 argv `git ... -C <original-checkout> merge --continue`이며 `--no-verify`/`--no-gpg-sign`을 추가하지 않고 기존 empty verified `core.hooksPath`, `commit.gpgSign=false`, null stdin을 재사용한다. 이 child 프로세스에만 검증된 author/committer 이름·이메일을 설정하며 저장·로그하지 않는다. **에디터 억제는 `GIT_EDITOR=true`로 한다** — 최초 설계 초안이 제시한 `GIT_MERGE_AUTOEDIT=no`는 실제 Git(2.48.1, Windows)에서 아무 효과가 없어 `merge --continue`의 내부 commit 단계가 Vim을 실행했고, 이 adapter가 stdin을 null로 주기 때문에 Vim이 즉시 오류를 내지 않고 무한히 대기하는 것을 실제 Git write 테스트로 확인했다. `GIT_EDITOR=true`에 대해 **검증된 것은 end-to-end 동작이지 특정 lookup 경로가 아니다**: 실제 임시 Git 저장소를 쓰는 `crates/chatoms-infrastructure/tests/merge_continue.rs`의 `continued_creates_the_exact_two_parent_commit_and_clears_residue`가 이 adapter의 controlled environment 그대로 `merge --continue`를 실행해, 이미 준비된 `MERGE_MSG`로 정확한 두 parent commit이 생성되고 잔여물이 제거됨을 확인한다. 표준 Git for Windows 레이아웃에서 `true.exe`는 설치 트리의 `usr\bin`에 있고 `mingw64\bin`에는 없다 — 이 문서의 이전 판이 후자로 단정한 것은 사실이 아니므로 정정한다. 따라서 resolution은 이 adapter가 설정하는 세 directory뿐 아니라 Git 자신이 자기 runtime prefix에서 child PATH에 덧붙이는 directory에도 의존한다. 어느 쪽이든 보안 속성은 동일하게 성립한다: adapter가 `env_clear` 후 PATH를 통째로 대체하므로 `true`가 resolve될 수 있는 모든 directory는 신뢰된 Git 설치 트리 또는 system directory에 속하며, 사용자 PATH·현재 CWD·task worktree에서는 절대 resolve되지 않는다. wall-clock timeout은 기존 Git write와 동일한 20초이고 cancellation은 지원하지 않는다(짧은 commit write를 중단하면 결과가 불명확해지기 때문). exit 0 후 정확한 두 parent, base branch 유지, `MERGE_HEAD`/`MERGE_MSG`/`MERGE_MODE`/`MERGE_AUTOSTASH` 제거, clean 상태, 그리고 post-commit stage-0 index digest가 confirmed digest와 다시 일치함을 모두 확인해야 `Continued`다. 그 외 모든 불확실성은 `RecoveryRequired`이고, 확인된 대기 중(`ConfirmedMergePending`)·stale confirmation(`ConfirmationStale`)은 `MergeConflict`로 되돌아가며 새 confirmation을 요구한다. |
| Post-merge provenance conflict-resolved chain | 확정 | `PostMergeTesting`의 `ProjectRoot` approval provenance resolver는 direct chain(`AwaitingUserDiffApproval → Merging → PostMergeTesting`)과 conflict-resolved chain(`AwaitingUserDiffApproval → Merging → MergeConflict → Merging → PostMergeTesting`) 둘 다 받아들이는 하나의 공유 resolver(`chatoms_application::merge_provenance`)로 통합했다. 두 chain 모두 sequence와 task_version이 완전히 연속이어야 하고 마지막 transition의 task_version이 현재 task version과 일치해야 하며, 최신 approval fallback이나 `current_version - N` 산술은 여전히 금지한다. |
| Merge-abort write contract | 확정 | `docs/STATE_MACHINE.md`가 이미 정의한 `MergeConflict → Cancelled` edge가 이 Unit 이전에는 노출된 command가 하나도 없어 도달 불가능했고, 안전한 도달 경로가 없으면 원본 checkout이 영구히 mid-merge 상태로 남는다는 근거로 이 경로를 구현했다. state machine과 domain `can_transition_to`는 변경하지 않았다 — abort 실패·불확실은 새 `RecoveryRequired` edge를 추가하는 대신 상태 전이 없이 `MergeConflict`를 그대로 유지한다. 승인 식별자는 `(task_id, merge_conflict_task_version)`이며 `task_manual_merge_resolution_confirmations`(0021)와 달리 resolution digest에 결합하지 않는다 — abort는 staged resolution을 폐기하는 동작이므로 digest 결합은 의미가 반대이고, 손상되거나 존재하지 않는 diff approval·`ProjectRoot` `Test`/`Build` 승인에 의존시키면 그것들이 이 상태의 유일한 탈출 경로를 막게 되므로 preflight는 이 둘을 요구하지 않는다. 유일한 Git write는 고정 argv `git ... -C <original-checkout> merge --abort`이며 `--quit`, `reset`, `checkout`, `restore`, `stash`, `clean`, `--no-verify`는 사용하지 않는다. `merge --abort`는 commit을 만들지 않으므로 author/committer 환경이나 `GIT_EDITOR`를 설정하지 않는다. 공식 `git-merge(1)` 문서(`git merge --abort is equivalent to git reset --merge when MERGE_HEAD is present unless MERGE_AUTOSTASH is also present in which case git merge --abort applies the stash entry to the worktree`)에 근거해 `MERGE_AUTOSTASH` 존재는 write 없이 항상 거부한다 — 그 stash를 적용하는 것은 미승인 내용을 원본 checkout에 쓰는 write이기 때문이다. 기존 20초 Git write timeout을 재사용하고 cancellation은 지원하지 않는다(짧은 abort write를 중단하면 결과가 불명확해지기 때문). `Aborted`(exit 0 + 복원 postcondition 성립)와 `ConfirmedNotInMerge`(write를 시도하지 않았거나 write가 실패했지만 복원 postcondition이 독립적으로 성립 — abort 성공과 SQLite commit 사이에 프로세스가 죽어 재시도가 필요한 경우의 유일한 복구 경로)만 `MergeConflict → Cancelled`와 `ActiveTaskLease` 해제를 커밋한다. 복원 postcondition은 merge 진행 중이 아니고, 어떤 잔여물도 없으며, 원본 checkout이 정확히 승인된 base commit에서 clean하고, task worktree가 승인된 task commit에 그대로 있음을 모두 요구한다. `PreWriteRejected`(identity/topology, autostash, foreign operation residue, 안전하지 않은 repository 설정, 승인된 값과 live 상태 간 merge identity 불일치, 또는 merge 중도 아니고 복원되지도 않음)와 `PostWriteUncertain`(exit 0인데 postcondition 불성립, 또는 nonzero/timeout/panic 등 그 외 모든 불확실성)은 상태 전이·history·lease를 전혀 바꾸지 않고 immutable approval만 남겨 재시도가 live 상태를 처음부터 다시 검증하게 한다. auto-abort, auto-retry, auto-cleanup은 추가하지 않았다. |
| Merge-conflict write mutual exclusion | 확정 | `MergeConflict`를 벗어나는 두 경로(merge-continue의 `git merge --continue`, merge-abort의 `git merge --abort`)는 같은 task의 동일한 original checkout에 쓰기 때문에 동시에 시작할 수 없다. 두 write는 서로 모순되며(하나는 staged resolution을 merge commit으로 확정하고 다른 하나는 폐기한다), 동시에 시작하면 두 Git 프로세스가 같은 index와 `MERGE_HEAD`를 두고 경쟁한다. 이를 Tauri 런타임의 process-local in-memory `MergeConflictWriteLock`(task id 단위 상호 배제)으로 막는다. 이 lock은 persistent state도, approval identity도, 상태 전이 marker도 아니며 오직 "지금 이 task의 merge-conflict write가 실행 중인가"만 나타낸다 — cancellation signal, task version binding, 영구 저장은 갖지 않는다(version 결합은 이미 immutable confirmation/approval이 담당하고, 두 command 모두 상태·버전을 transaction 안에서 다시 검증한다). 기존 `MergeAbortRunRegistry`(같은 task의 *두 번째 abort* 방지)는 일반화·개명·통합하지 않고 그대로 두며, 시작된 abort는 둘 다 보유한다. 두 command 모두 cheap Git-free fail-fast 직후·`GitCliAdapter::from_environment()` 이전에 lock을 획득하므로, 거부된 호출은 confirmation/approval 기록, `MergeConflict → Merging` 전이, Git write, background thread를 하나도 시작하지 않는다. merge-continue는 content-free typed `Conflict` 오류로, merge-abort는 기존 중복 abort와 동일한 `{ started: false }`로 거부한다. 해제는 전적으로 RAII 책임이다: 동기 단계 실패, background thread 정상 종료, recorder 오류, uncertain outcome, panic unwind 어느 경로에서도 해제되며 lock이 recovery marker로 남는 일은 없다. app restart는 lock을 함께 소멸시키는데 이것이 정확한 동작이다 — 배제할 실행 중 Git 프로세스가 남아 있지 않고, task 상태 회수는 기존 `TaskService::reconcile_startup_merge`가 담당한다(lock은 여기에 참여하지 않는다). merge write cancellation, retry, 두 registry 통합은 도입하지 않았다. |
| Merge write observation과 실패 처리 | 확정 | (1) **읽기 전용 관찰은 index lock을 잡지 않는다.** 공용 `GitService::repository_status`의 고정 argv는 `--no-optional-locks`(global option이므로 subcommand 앞)를 포함한다. 이 helper의 모든 호출자가 read-only 관찰자이고(project inspection, merge execution/continue/abort의 pre-/post-write 검증, merge-conflict inspection), 그 중 merge-conflict inspection은 abort write가 같은 original checkout에서 실행되는 내내 2초 간격 UI polling으로 호출된다 — 이 flag가 없으면 성공했어야 할 abort가 index lock 경합으로 `PostWriteUncertain`이 될 수 있다. mutation command에는 적용하지 않는다(그쪽은 정당하게 index lock을 잡는다). shell 문자열은 만들지 않으며 argv는 named constant로 고정하고 순서를 테스트로 고정한다. (2) **최초 merge의 residue 집합과 성공 postcondition은 merge-continue와 동일 수준이다.** prewrite residue 검사는 `MERGE_HEAD`/`MERGE_MSG`/`MERGE_MODE`/`MERGE_AUTOSTASH` 넷을 모두 보고 하나라도 있으면 `git add`·task commit·`git merge` 중 무엇도 시작하지 않는 `ExistingMergeResidue`로 거부한다. 성공 판정은 parent 목록만으로 하지 않고 original checkout이 expected base branch에 있고, `HEAD`가 방금 관찰한 그 merge commit이며, 그 commit의 parent가 정확히 (approved base commit, task commit) 순서 두 개이고, merge residue가 없으며, clean함을 모두 요구한다. 하나라도 확인되지 않으면 성공으로 추정하지 않고 기존 `PostWriteUncertain` 경로로 보낸다. 이 검증이 읽는 commit hash·path·Git output은 boolean 외에 어떤 DTO·UI·error에도 노출되지 않는다. (3) **background thread 생성 실패는 성공으로 추정하지 않고 hold를 누수하지 않는다.** 세 merge command 모두 `std::thread::spawn`(생성 실패 시 panic) 대신 `std::thread::Builder::spawn`의 `Result`를 쓴다. merge-continue는 guard를 closure로 이미 이전했으므로 `spawn`이 `Err` 경로에서 closure를 drop하면 `Drop`이 lock을 해제한다. merge-abort는 closure가 guard를 *생성*하는 구조라 drop만으로는 아무것도 해제되지 않으므로 `start_locked`가 `Err`를 반환해 호출부의 기존 error arm이 registry entry와 write lock을 함께 돌려준다. 이미 상태 전이를 커밋한 두 경로(merge execution의 `Merging`, merge-continue의 `MergeConflict → Merging`)는 현재 persisted 상태·버전에 대해 기존 fail-closed recovery outcome을 기록한 뒤 typed 오류를 반환한다. raw spawn 오류는 IPC·UI로 전달하지 않는다. |
